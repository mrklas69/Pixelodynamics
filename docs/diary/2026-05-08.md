# 2026-05-08

## Sezení 8 — Fáze 3 entry: FixedJoint API, audio, edge mask, sleep-mode fix

### Diskuse

- @BEGIN audit: 7 sezení dosud, žádný práh nepřekročen. Žádné stale Příště — Pages deploy a centroid křížek byly DONE v sezení 6/7.
- Volba (E) FixedJoint API + E3 + (α/β/γ) test ze sezení 7 Příště. α/β/γ má smysl jen s gravitací aktivní; E3 má G=0 → α/β/γ degeneruje na pure rapier. Stage 1 = joint API + audio + visualization + E3 funkční baseline; Stage 2 (α/β/γ s G=1 + joint) = samostatné navazující sezení.
- Q1: anchor (0,0)/(0,0) vs. edge-to-edge (+0.5,0)/(-0.5,0)? → A1 hrana-na-hranu (fyzicky odpovídá „dotyk po straně").
- Q2: detekce dotyku po straně teď, nebo TODO? → A2 TODO.
- Q3: zvuk při joint create i spawn, nebo jen joint? → A3 jen joint (uživatel dodá spawn sound).
- Po E3 modelshotu uživatel poskytl JSON: `vx=vy=0`, `r=0.605`, KE=0 — dramatická dissipace. Diagnóza: oba momenty (linear i angular) dekomprimovány exponenciálně s `λ ≈ 0.5/s`, half-life ~1.4 s. Hypotéza: Rapier sleep mode + PGS solver Baumgarte korekce.
- Volba (a) E3-tune mini-experiment k izolaci sleep-mode od solver-iteration efektu.
- Po E3-tune výsledcích: uživatel souhlasí s `canSleep=false` jako default + chce skrýt vnitřní hrany spojených pixelů („obrysy komponentů, ne dvojice čtverečků"). Po implementaci s `discard` se objevily černé zobáčky v rozích → druhá iterace na 50% průhlednost → po dalším testu finální volba červená barva (#d86f6f).
- Test distantního spojení (~5 U od sebe) odhalil vizuální snap při create — pixely viditelně poskočily, aby vyrovnaly orientaci. Fix: `JointData.fixed` `frame1` parametr.

### Rozhodnutí

- **Stage 1 scope:** FixedJoint API + audio (click + spawn) + edge mask vizualizace + sleep-mode fix. **Stage 2 (α/β/γ) odloženo** na další sezení — rapier joint solver po tuningu **funguje**, takže hybrid orchestrace má jasný cíl: kompenzovat **gravity-side** dissipaci, ne joint-side.
- **`World.defaultCanSleep = false`** jako Pixelodynamics-wide default. Sleep mode je herní optimalizace, ve fyzikálním sandboxu **anti-feature** — uspí rotující rigid pair a tichne dynamics. Žádná regression na E1–E7 (manuální mode nebo bodies bez constraint nikdy nebudily sleep threshold tak agresivně).
- **Edge mask vizualizace:** 4-bit mask per pixel jako WebGL per-instance attribute. Maskovaná hrana se kreslí červeně (`#d86f6f`), ne discard. Důvody:
  - Plný discard rohových fragmentů (corner kde v_local ≈ (±0.5, ±0.5)) vytvořil černé „zobáčky" — corner má dPosX==dPosY==minDist, jakákoli z nich masked → discard celý roh.
  - 50% průhlednost (mezikrok) byl měkčí, ale uživatel preferuje explicitní vizuální signál (červená).
  - Long-distance jointy (anchor mimo obě tělesa) vytvářely U-shapes při full discard; červená barva je vizuální hint na spoj bez geometrie destrukce.
- **Joint orientation snap fix:** `JointData.fixed(anchorA, r_B − r_A, anchorB, 0)` místo `(0, 0)`. Frame parametry kódují **relativní rotaci constrainu**: `r_A + frame1 == r_B + frame2`. S nulami bylo `r_A == r_B`, což pro LMB-spawn pixely s random rotacemi violation → solver instant impulse → snap. Fix preserve aktuální rotation gap.
- **Naming:** zvuk pro joint create i remove je stejný (`click.mp3`) — symetrie eventů. Spawn LMB má vlastní zvuk (`spawn.mp3`).
- **E3 preset finální podoba:** canSleep=false + solverIters=16 + pgsIterations=4 (tuned). Drift po 10 s ~0.01% na ω, ∑P k f32 epsilon. Smazány E3 (broken default) a E3s (mise splněna).

### Kód

**Audio (`src/audio/sfx.ts`):**
- Pool 5 `HTMLAudioElement` instancí per zvuk, round-robin. Lazy init při prvním play. Volume 0.4. Autoplay-block tiše ignorován (catch).
- API: `playClick()` (joint create+remove), `playSpawn()` (LMB).
- `Map<string, Pool>` storage — refactor po přidání druhého zvuku.

**Joint API (`src/sim/joints.ts`):**
- Typ `Joint = { id, a, b, anchorA, anchorB, rapier }`.
- `createFixedJoint(world, a, b)`:
  - midpoint = (posA + posB) / 2 (world)
  - anchor v lokálním frame = `R(-rot) · (midpoint - pos)`
  - `JointData.fixed(anchorA, rB - rA, anchorB, 0)` — preserve current relative orientation
  - playClick()
- `removeJoint(world, joint)` + playClick().
- `removeAllJointsSilent(world)` — pro reset (žádný N-click noise).
- Edge-to-edge anchor: pro pixely 1 U od sebe leží anchor přesně na sdílené hraně (`anchorA = (+0.5, 0)`, `anchorB = (-0.5, 0)`).

**Physics rozšíření (`src/sim/physics.ts`):**
- `World.joints: Joint[]` paralelní storage k Rapier impulse joint set.
- `defaultCanSleep = false` + `setDefaultCanSleep(b)` toggle.
- `setSolverIterations(n)`, `setPgsIterations(n)` — IntegrationParameters tuning.
- `clear()` removuje jointy před pixely (Rapier auto-clean by jointy odstranil, ale naše paralelní storage by ostala stale).

**Presety (`src/sim/presets.ts`):**
- `PresetAPI.spawn` → vrací `Pixel` handle (preset může referencovat).
- `PresetAPI.connect(a, b)` → wrapper kolem `createFixedJoint`.
- `PresetAPI.tuneRapier({ solverIterations?, pgsIterations?, canSleep? })` — per-preset tuning.
- `applyPreset` resetuje tunings na default před `preset.setup` (jinak by E3+ tuning přetrval do dalšího presetu).
- E3 přepsán na working baseline (anchors edge-to-edge, vy=0.5 na pravém, tuned).

**Edge mask vizualizace (`src/render/gl.ts` + `App.svelte`):**
- Renderer: nový per-instance `a_edgeMask` (location 3, float kódovaný 4-bit). `maskBuffer`, `maskData`. VAO setup + bufferData + bufferSubData.
- VS: `flat out int v_edgeMask` (no interpolation).
- FS: jakmile fragment je „border" (`minDist <= u_borderHalfWidth`), zkontroluj nejbližší hranu vůči masce. Pokud match → `col = jointCol = #d86f6f`, jinak default. Bez discard.
- App.svelte: `edgeBit({x,y})` helper — dominantní lokální osa anchoru → 4-bit signál (+X=1, -X=2, +Y=4, -Y=8).
- Per-frame mask compute: O(N) reset + O(J) přes Map<Pixel, idx>.
- Alpha blending povoleno v rendereru (`SRC_ALPHA, ONE_MINUS_SRC_ALPHA`) — pro budoucí semi-transparent features; nyní nepoužitelné (red má alpha=1).

**UI (`App.svelte`):**
- Tlačítka v COMMANDS: 🔗 Spojit poslední 2 pixely, ✂ Rozpojit vše (disabled při 0 jointech).
- `connectionCount` wire-up (display tick + applyPreset + resetScene + manuální tlačítka).
- Spawn click hook v `onPointerDown`.
- HMR přes Vite — žádný restart serveru potřeba.

**`@BEGIN`/`@END` makro update (`CLAUDE.md`):**
- Dev server jako **detached proces** přes `Start-Process cmd /c npm run dev -WindowStyle Hidden`. PID file v `.claude/dev-server.pid` (gitignored).
- Server přežije ukončení Claude Code session a `/clear`.
- @BEGIN vypisuje URL `http://localhost:5173/` jako klikací odkaz.
- @END kill přes `taskkill /T /F` celý strom.

### Test/ověření

**E3 finální baseline (canSleep=false, solverIters=16, pgs=4):**
- Modelshot @ t=10s:
  - A: (0.99496, 2.4292, vx=0.0213, vy=0.3984), r=2.9996
  - B: (0.00503, 2.5708, vx=-0.0213, vy=0.1016), r=2.9996
  - rs=0.29991 (vs analytical 0.3 → drift 0.03%)
  - L=0.24993 (vs 0.25 → drift 0.03%)
  - KE=0.09998 (vs 0.1 → drift 0.02%)
  - ∑P_y=0.4999983 (vs 0.5 → f32 epsilon)
- Analytical reference (pixel-square inertia I=m/6, I_pair_cm=5/6):
  - ω = L/I = 0.25/0.833 = **0.3 rad/s** exact
  - cm @ t=10: (0.5, 2.5), pair angle = 3 rad
  - A: (0.995, 2.429), B: (0.005, 2.571)
- **Bit-shoda na 4 platné číslice** ve všech metrikách. Rapier joint solver jako reference použitelný.

**E3-tune dekompozice:**
| Preset | canSleep | iters/PGS | r drift | L drift | Verdikt |
|---|---|---|---|---|---|
| E3 (default) | true | 4/1 | -80% | -100% | sleep killed it |
| E3s | false | 4/1 | -0.06% | -0.12% | matches |
| E3t | false | 16/4 | -0.01% | -0.03% | best |

→ Sleep mode = 100% viník katastrofické dissipace v default E3. Solver iterations dají další řád přesnosti.

`npm run check`: 0 errors, 0 warnings.

### Censure / Kudos

**Kudos! uživateli (E3 modelshot diagnostika):** Bez explicitního modelshotu z první iterace E3 bych si toho efektu sleep mode nevšiml — pure rapier mode jsem v sezení 3 testoval s G=0 a nulovými velocities (E1/E2), kde sleep nebyl viditelně škodlivý. Joint+pohyb scénář je první případ, kde se to provalilo. Kudos taky za rychlou A1/A2/A3 odpovědi — žádné back-and-forth.

**Kudos! uživateli (zachycení snap při distantním joinu):** Bug s `JointData.fixed(0, 0)` framem by se mohl skrývat dál — pro E3 (oba pixely r=0) byl tichý. Random LMB spawn s nenulovou rotací byl skutečný stress test, kde se chyba projevila viditelně. Default to **adversarial test scenarios** s random initial conditions.

**Censure! sobě:** Predikce E3 baseline („pure rapier baseline pro porovnání s α/β/γ") byla fundamentálně chybná — neuvědomil jsem si, že sleep mode je v default Rapier configu zapnutý a může uspat tělesa s pomalým pohybem. Sezení 2 censure (Rapier integrátor degraduje orbital dynamics) předpovídala obecný problém, ale specificky pro joint scénář jsem nepředvídal sleep jako mechanismus. **Default Rapier configuration audit** by měl být součástí @BEGIN setupu pro každou novou Rapier-based feature.

**Censure! sobě (edge mask iterace):** První návrh `discard` masked edges byl matematicky správný, ale ne vizuálně domyšlený — corner fragmenty (kde dvě hrany jsou stejně blízko) discardly celé rohy, což vytvořilo „černé zobáčky". Sezení 4 mělo censure o **algebraické ověření na nenulovém vstupu**; to platí i pro **vizuální ověření na corner cases**. Při psaní shaderů: vždy mentálně procházet rohy, ne jen středy hran.

**Censure! sobě (joint frame parametr):** `JointData.fixed(anchorA, 0, anchorB, 0)` byl signature lookup, ale nedočetl jsem doc string o významu frame parametrů. Default 0/0 demanduje r_A == r_B, což je sub-problem že to se shoduje s E3 setup (oba r=0) → tichá chyba pro E3, viditelná jen pro random LMB. **API kterého doc je dostupné, číst k odstranění tichého fallthrough.**

### Příště

- **Stage 2 (E) — α/β/γ design.** Nový preset E8 s `G=1` + 2 free pixely v gravitaci + 1 pinned attractor + FixedJoint mezi 2 free. Implementace tří hybrid IntegrationMode hodnot. Měření: pos / KE / ∑P drift po N krocích vs. analytical (eliptická orbit) nebo vs. čistý rapier baseline (po E3-tune víme, že rapier joint solver je trusted reference — joint dynamika je správná, jen gravitace v rapieru by byla broken).
- **Auto-detekce dotyku po straně** — collisionGroups zapnout, contact event listener, automatický `createFixedJoint` při kontaktu se sousedy. Vede k organickému slepování bez manuálního tlačítka.
- **Composite object dataset** (IDEAS) — postupně budovat z existujících jointů + pixelů, jakmile budou auto-vznikat.
- **Spawn sound balance** — současně 0.4 volume může být v rapid-fire LMB scénáři rušivé. Volume slider v SETTINGS jako budoucí dluh.

## Sezení 9 — Stage 2: hybrid orchestrace α/β, E8 sweep, α etablováno jako default

### Diskuse

- @BEGIN audit: 8 sezení dosaženo, `@AUDIT:CODE` práh přesně dosažen (ne překročen ≥2). Doporučeno jako jeden z bodů, ne forced first. Stage 2 zvolena jako primární cíl (kontinuita ze sezení 7→8 Příště).
- Q1: reference baseline pro E8? → A1 pure rapier (po E3-tune trusted pro joint solver); explicitně vědomí, že rapier gravity-side je broken (sezení 2).
- Q2: implementovat všechny tři (α/β/γ) najednou, nebo postupně? → A2 všechny tři + IntegrationMode toggle.
- Q3: γ jako flag, samostatný mód, nebo později? → A3 flag `SKIP_RAPIER_IF_NO_JOINTS=true` (orthogonal optimization). γ není self-contained — dispatchuje na α nebo β; degenerace na manual když joints=0.
- Q4: E8 attractor design? → A4 1 pinned (m=10) + 2 free joint pair (m=1 each). Asymetrie umožňuje jednoduchou orbit geometrii kolem fixed centra.
- Po implementaci E8 (E8r/E8α/E8β) modelshoty odhalily:
  - **E8r broken pro orbit**: pure rapier nevolá `stepGravity`, gravita = external force kterou Rapier nezná → free pair letí balisticky ve směru initial vy (pos y=42.4 po 30 s místo orbit). Joint solver jediný, který funguje (distance konstant). E8r baseline použitelný pouze pro ověření, že joint constraint sám zachovává distance, NE pro porovnání orbit dynamics.
  - **E8α: ∑E drift 0.04 % / 30 s** (init −2.418 → final −2.417). Joint distance ~0.99. Pair se otáčí rs=0.32 rad/s (tidal locking). **Working.**
  - **E8β: ∑E drift 13 % / 30 s** + **rs ≈ 0** (pair se netočí). **Broken.**
- Příčina patologie β: nulování linvel/angvel před `world.step()` znamená, že rapier joint solver nevidí gravitační stress (manual ho už aplikoval do pos, ale Rapier ho nezná) → nedá tidal torque → addback delta = ~0. Plus position-based Baumgarte korekce na joint constraint po nulové vel přes f32 round-trip dissipuje energii (13 %/30 s).

### Rozhodnutí

- **α (Velocity-Verlet split) etablovat jako default hybrid mode** pro fázi 3+. UI default: `'hybrid-α'` (původně `'manual'`). γ flag (`SKIP_RAPIER_IF_NO_JOINTS=true`) automaticky degeneruje na pure manual když `world.joints.length === 0`, takže existující čistě gravitační scénáře (E5m/E6/E7/PB) přechod nepocítí.
- **β archivovat jako tested+rejected.** Kód `saveZeroVel`/`restoreVelDelta` zachován v `physics.ts` pro reprodukovatelnost a možný budoucí refinement (např. nenulovat angvel separátně), ale E8β se stane historickým censure marker.
- **`hybrid-naive` zachováno** pro reprodukci E5 (sezení 3). Současný E5 přepsán z `'hybrid'` na `'hybrid-naive'`.
- **γ flag SKIP_RAPIER_IF_NO_JOINTS** = orthogonal optimization, default `true`. Bez constraintů = bypass Rapier step → konzervace na úroveň pure manual. S jointy → full hybrid-α/β. Když přijde auto-detekce dotyku po straně (TODO), γ se aktivuje organicky.
- **Naming řeckými písmeny α/β** v IntegrationMode (string union value, ne enum identifier — TS string union dovolí non-ASCII). Konzistentní s diary/IDEAS terminologií.

### Kód

**`src/sim/gravity.ts`:**
- Refactor: `stepGravity` rozdělen na `stepGravityFull` (current, full kick+drift+rotation+writeback) a **nová `stepGravityKickOnly`** (jen kick, write linvel; pos a rotation drift dělá Rapier `world.step()`). Sdílený `readState` + `accumulateAccel` interní helper.
- `stepGravityKickOnly` použito pro hybrid-α; pro pinned skip stejný jako full.

**`src/sim/physics.ts`:**
- `SavedVel` typ + `saveZeroVel(world)` / `restoreVelDelta(world, saved)` pro β. Save: ulož `(linvel, angvel)` non-pinned, set 0/0 v Rapier body. Restore: addback saved + current (= delta z constraint impulses).

**`src/sim/params.ts`:**
- `SKIP_RAPIER_IF_NO_JOINTS = true` — γ flag.

**`src/sim/presets.ts`:**
- `IntegrationMode` rozšířen z `'manual' | 'rapier' | 'hybrid'` na **`'manual' | 'rapier' | 'hybrid-naive' | 'hybrid-α' | 'hybrid-β'`**.
- E5 přepsán na `'hybrid-naive'`.
- **Tři E8 varianty** se shared `e8Spawn(api)`:
  - E8r — `'rapier'` (broken baseline, vědomě)
  - E8α — `'hybrid-α'`
  - E8β — `'hybrid-β'`
- Common spawn: pinned attractor (M=10) v (0,0), free pair (m=1+m=1) na (R±0.5, 0) s tečnou v_circ=√(GM/R)≈1.414, R=5. FixedJoint mezi free. tuneRapier solver=16/PGS=4/canSleep=false.

**`src/ui/App.svelte`:**
- Main loop: `switch(integration)` nad pěti módy. γ flag check (`skipRapier = SKIP_RAPIER_IF_NO_JOINTS && w.joints.length === 0`).
- Hybrid-α s skipRapier degeneruje na full manual (kick-only by jen akumuloval vel bez Rapier driftu).
- UI: nový dropdown `<select bind:value={integration}>` v SETTINGS (po cutoff slideru). Live override; preset reset overríde.
- CSS: `.select` styl (analogie `.slider`).
- Default `integration` z `'manual'` na `'hybrid-α'`.

### Test/ověření

E8 sweep modelshot @ 30 s:

| Preset | mode | pos pixel L | KE | PE | ∑E | drift ∑E | rs free pair |
|---|---|---|---|---|---|---|---|
| E8r | rapier | (4.5, 42.4) | 2.000 | 0 | 2.000 | — (gravita chybí) | 0 |
| E8α | hybrid-α | (2.92, 4.56) | 1.486 | -3.904 | -2.417 | **0.04 %** | 0.322 |
| E8β | hybrid-β | (3.56, 3.99) | 1.957 | -4.058 | -2.102 | **13 %** | -2.3e-5 |

Init reference (analytical): KE_init = 2·½·1·1.414² = 2.0; PE_init = -2.108 (L attract) + -1.755 (R attract) + -0.555 (pair-pair) = -4.418; ∑E_init = -2.418.

E8α match na 0.04 % od initial — úroveň truncation symplektického Eulera. Joint distance po 30 s ≈ 0.99 (preserve). Pair se otáčí rs=0.32 (gravita-induced precesní moment).

E8β: rs ≈ 0 znamená rapier joint solver nedal tidal torque (vel=0 maskovala stress). Plus 13 % ∑E leak.

`npm run check`: 0 errors, 0 warnings (sezení 9 final).

### Censure / Kudos

**Censure! sobě (E8r baseline mismodelovaný):** Navrhl jsem E8r = "trusted reference" pro α/β, ale neuvědomil jsem si, že pure rapier mód **nezavolá `stepGravity`** vůbec. Free pair tedy v E8r letí balisticky bez gravity. Sezení 2 censure (rapier orbital broken) měla generální platnost, ale specificky pro E8 jsem si neudělal mental run-through "co se stane když Rapier dostane initial vy a žádnou external force". Lekce: **mental simulation každého presetu PŘED spuštěním**, ne pouze po modelshotu.

**Censure! sobě (β by se mělo predikovat broken):** Patologii β bylo možné předvídat z první principů: nulování velocity před joint solverem znamená, že solver nemá kontext o stress. Místo toho jsem implementoval β paralelně s α, abych "viděl, co se stane". To je **empirický defaulting** místo myšlenkového experimentu. Sezení 4 censure (sanity check matic při psaní) se vztahuje i na **algoritmické sanity check** — projít algoritmus krok po kroku na 1 jointu před implementací.

**Kudos! uživateli (rychlé Q1–Q4 cycle):** Bez back-and-forth, čisté volby. Jeden z mála sezení, kde čas mezi otázkou a kódem byl minimal.

**Kudos! sobě (E8 sdílený spawn):** Refactor `e8Spawn(api)` jako sdílený helper a tři varianty pres. Bit-by-bit identický initial state mezi E8r/E8α/E8β znamená, že rozdíly v modelshotech jsou jen z integrátoru, ne z setup. Tahle disciplína vznikla v sezení 5 (E7n/E7g sdílený spawn) a aplikuje se izomorfně.

### Příště

- **Auto-detekce dotyku po straně** — collisionGroups + contact event listener → automatický `createFixedJoint` při kontaktu sousedů. γ flag se začne organicky aktivovat. Hlavní kontinuita Stage 3.
- **Composite object dataset** (IDEAS) — postupně budovat z existujících jointů + pixelů, jakmile budou auto-vznikat.
- **β archiv** — eventuálně promazat E8β preset, pokud se nedotkneme β refinementu (např. partial save: jen linvel, ne angvel). Zachovat 2-3 sezení jako historický signál, pak mažeme.
- **Spawn sound balance** (carry-over ze sezení 8).

## Sezení 10 — Stage 3a: auto-jointing při kontaktu, object counter

### Diskuse

- @BEGIN audit: 9 sezení dosud, `@AUDIT:CODE` práh dosažen ale ne překročen ≥ 2 → doporučeno, ne forced. Žádné stale Příště. Volba auto-detekce dotyku po straně jako primární kontinuita Stage 3 ze sezení 9.
- Q1: trigger pro joint create (Started / klidový kontakt / Started + cooldown)? → A1 hned při kontaktu (Started event).
- Q2: test scénář — head-on, 3-pixel řada, ring 8? → A2 head-on (nejjednodušší, nejdiagnostičtější).
- Po implementaci collision groups + ActiveEvents + drainAndAutoJoint:
  - **E10 broken**: pixely proletěly skrze sebe (žádný contact event). Auto-joint nefíroval, rel velocity zachována.
  - **E3 drift -11.5 %**: rs 0.265 vs. sezení 8 baseline 0.300, L drift identical, KE drift -8 %. Joint solver konflikt s contact response.
  - **E8α drift 1.2 %**: ∑E -2.446 vs. init -2.418 (sezení 9 baseline 0.04 %).
- Diagnóza:
  - **E10**: `SKIP_RAPIER_IF_NO_JOINTS=true` v hybrid-α s prázdným `joints[]` skipuje `rapier.step()` celkem → manual Euler integruje pos sám. Bez `rapier.step()` Rapier broadphase neaktualizuje, žádný Started event. Auto-joint vyžaduje broadphase běh — γ flag a auto-joint si **navzájem konflikt**.
  - **E3 / E8α**: zapnutí `setCollisionGroups(0xFFFFFFFF)` + `setActiveEvents(COLLISION_EVENTS)` aktivovalo narrow-phase pro edge-touching pixely. Rapier teď řeší **contact normal force** paralelně s **joint constraint**, oba si škvaří energii (joint constraint demanduje pos preserve, contact normal demanduje neghraniční penetration).
- Fix:
  - **γ flag zohlední auto-joint**: `skipRapier = SKIP_RAPIER_IF_NO_JOINTS && joints.length === 0 && !AUTO_JOINT_ON_CONTACT`. Pro hybrid-α/β s AUTO_JOINT=true se rapier.step() volá vždy, broadphase běží, auto-joint funguje.
  - **`joint.setContactsEnabled(false)`** v `createFixedJoint` — Rapier API explicitně říká "vypni contact mezi joined bodies, joint je jediná autorita".
- Po fixu: E10 ✓ (KE→0 head-on collision + slepenec), E3 ✓ (bit-identical), E8α ✓ (drift 0.04 %). PB1000 po 11 s @ 58 FPS (acceptable).
- Uživatel upozornil, že STATS Objects = 0 i po slepení (counter se nikde neinkrementuje). Implementace `computeObjectCount` přes Union-Find s path compression a union by size. O((N+J)·α). Wire-up v display ticku.
- ✂ Rozpojit v hybrid-α + auto-joint: po removeJoint pixely v kontaktu znovu emit Started → re-joint okamžitě. Logický důsledek "hned při kontaktu" volby. Ne bug, vlastnost.

### Rozhodnutí

- **`AUTO_JOINT_ON_CONTACT=true` jako default** — Stage 3a ošetřena. Toggle v `params.ts` pro reprodukci pre-Stage-3a presetů (E5 hybrid-naive nyní změní chování: pixely v kolapsu se začnou organicky lepit).
- **Collision groups na pixelech vždy `0xFFFFFFFF`** — všichni se všemi. Pro budoucí ne-lepivé kategorie (různé barvy, sensors) lze použít user-data nebo runtime check v contact handleru.
- **`joint.setContactsEnabled(false)` jako default** v `createFixedJoint` — joint authoritu nad rel pos. Žádný použito-case dosud nepotřebuje contacts mezi joined bodies (= self-collision skrz joint pole).
- **γ flag s auto-joint kompatibilní** — `SKIP_RAPIER_IF_NO_JOINTS` jako orthogonal optimization přestala být orthogonal jakmile přibyl feature, který vyžaduje broadphase běh. Side-effect dependency, ne čistý design. Ale `manual` mode pro pure-gravita presety (PB, E5m, E6, E7) zůstává unchanged — auto-joint v nich se přirozeně **neaktivuje**, protože `manual` nikdy nevolá `rapier.step()`.
- **Edge case ✂ Rozpojit + auto-joint**: zachováme current chování, vlastnost ne bug. Pokud se uživatel rozhodne, lze později přidat per-pair cooldown blacklist (memory + timer). Pro teď: Rozpojit v rapidním cyklu = no-op, dokud něco nepohne pixely od sebe.
- **Object counter pres Union-Find** (přesně počet komponent) místo aproximace `pixelCount - jointCount` (která selhává pro cykly). O((N+J)·α) per display tick = 500 ms. Pro N=1000, J=500 zanedbatelné.

### Kód

**`src/sim/params.ts`:**
- `AUTO_JOINT_ON_CONTACT = true` flag.

**`src/sim/physics.ts`:**
- `pixelByCollider: Map<number, Pixel>` — O(1) lookup z collider handle.
- `eventQueue: RAPIER.EventQueue` (autoDrain=true) v `init()`.
- `setCollisionGroups(0xFFFFFFFF)` + `setActiveEvents(COLLISION_EVENTS)` v `spawnPixelExact`.
- `pixelByCollider.set(collider.handle, pixel)` při spawn, `.clear()` v `clear()`.
- `step(dt)` přidá `eventQueue` parametr → `rapier.step(eventQueue)`.
- Nová `drainContactStarts(callback)` — drainuje Started events, callback s lookupem A/B pixely, ignore Stopped.

**`src/sim/joints.ts`:**
- `rapier.setContactsEnabled(false)` po `createImpulseJoint` v `createFixedJoint`.

**`src/ui/App.svelte`:**
- Helper `drainAndAutoJoint()` v main loopu (po `switch(integration)`). Duplicate guard přes lineární scan `world.joints.some()`.
- γ flag fix: `skipRapier = SKIP_RAPIER_IF_NO_JOINTS && w.joints.length === 0 && !AUTO_JOINT_ON_CONTACT`.
- `objectCount = computeObjectCount(w)` v display ticku + reset na 0 v `resetScene`.

**`src/sim/diagnostics.ts`:**
- `computeObjectCount(world): number` — Union-Find s path compression a union by size, vrací počet komponent. O((N+J)·α).

**`src/sim/presets.ts`:**
- E10 — Auto-joint head-on (2 pixely vx=±0.5, G=0, stop @ 8 s).
- E11 — Auto-joint trio (3 pixely v řadě, G=1, stop @ 12 s).

### Test/ověření

| Preset | Metrika | Aktuální | Baseline | Status |
|---|---|---|---|---|
| E10 | KE @ 8s | 0 | — (nový) | inelastic kolaps OK |
| E10 | distance | 0.9997 U | 1.0 U | joint preserve ✓ |
| E10 | Objects (UI) | 1 | — | union-find ✓ |
| E11 | KE @ 12s | 5.5e-17 | — (nový) | machine epsilon ✓ |
| E11 | pos #0/#1/#2 | -1 / 0 / +1 | — | symetrický kolaps ✓ |
| E11 | jointCount | 2 | — | řetězec ✓ |
| E3 | rs | 0.29991 | 0.29991 (S8) | **bit-identical** |
| E3 | L | 0.24993 | 0.24993 (S8) | **bit-identical** |
| E8α | ∑E drift | 0.04 % | 0.04 % (S9) | match |
| E8α | rs free | 0.322 | 0.322 (S9) | match |
| PB1000 @ 11s | FPS | 58 | 20 (S7, post-collapse manual) | acceptable, ne přímé srovnání |

`npm run check`: 0 errors, 0 warnings.

### Censure / Kudos

**Censure! sobě (E10 broken — γ vs auto-joint conflict):** Implementoval jsem `AUTO_JOINT_ON_CONTACT` aniž bych si uvědomil, že **γ flag bypassuje rapier.step()** v hybrid-α bez jointů. Auto-joint vyžaduje broadphase běh — bez `rapier.step()` Rapier nikdy nevidí, že se pixely přiblížily. Mental simulation: "co se stane, když 2 pixely letí proti sobě v hybrid-α s skipRapier=true" jsem **neudělal** před implementací. Sezení 9 censure (mental run-through každého presetu) se vztahuje i na **mental run-through každého feature x flag combination** před prvním testem. Bug se projevil jasně (E10 pixely proletěly skrze sebe), takže fix byl rychlý, ale to neomlouvá chybějící premisu.

**Censure! sobě (E3 drift — joint+contact dual constraint):** Zapnul jsem collision groups na všech pixelech, aniž bych explicitně vyřešil, **co se stane mezi joined pixely**. Edge-touching pair v E3 dostal contact constraint **paralelně** s joint constraint. Rapier API dokumentuje `setContactsEnabled(false)` na joint pro tuhle situaci, ale defaulty **collisions povolené i mezi joined**, což je opačné než physics simulation potřebuje. Sezení 8 censure (audit Rapier defaults) se vztahuje i na **per-feature defaulty**, ne jen World-level. Fix triviální (1 řádek), ale baseline regrese -11.5 % vs. 0.03 % byl velký signál.

**Censure! sobě (object counter pre-existing dluh):** `objectCount = $state(0)` byl v UI od sezení 7+, ale nikdy se neinkrementoval. Uživatel to upozornil až teď. Měl jsem ho dotáhnout v sezení 8 (FixedJoint API), kde už mělo smysl — auto-joint to teprve eskaloval. **Hardcoded 0 v UI = silent indicator, že feature není kompletní**; tohle by mělo automaticky vyvolat audit.

**Kudos! uživateli (PB1000 perf zachycen):** FPS sledování v reálném testu je důležité pro perf regression detection. Bez explicitního "FPS 58" by mě nenapadlo, že auto-joint v PB1000 nezhroutil výkon. Connection counter se inkrementoval do stovek, contact events se musely drainovat každý tick, a to nezhroutilo to. Solid empirický důkaz, že náš implementace nebyla naivní.

**Kudos! uživateli (object counter feature request):** Nesplnil jsi naopak roli QA poukazem na "STATS Objects = 0 i po slepení" — to byl chybějící kus, který by jinak ostal v dlouhodobém TODO bez user-visible signal. Push k dokončení teď byl správný.

**Kudos! sobě (rychlá oprava obou bugů):** E10+E3 oba bugy jsem identifikoval z modelshotů + statické analýzy bez další iterace s uživatelem. setContactsEnabled(false) + γ flag fix dohromady 5 řádků kódu, type check prošel napoprvé, oba presety se ve druhém pokusu bit-identicky shodly s sezení 8/9 baseline. Tohle je správný flow: empirické signály → diagnóza → minimal fix → verify proti baselině.

### Příště

- **Composite object dataset** (IDEAS) — postupně budovat z existujících jointů + pixelů. Object counter máme, dataset (cached těžiště, hmotnost, moment setrvačnosti pro každý slepenec) je další krok. Údaj `Largest` v FACTS panelu se napojí.
- **Edge case detekce: pair-to-pair kontakt** — pokud dva slepenci se srazí, contact mezi pixely, které jsou každý v jiném slepenci, by měl auto-jointovat → ze 2 slepenců 1 větší. Současný kód toto **už podporuje** (duplicate guard se týká pixel pair, ne object pair), ale stojí to za empirický test (např. E12 — dva head-on slepence m=2 každý).
- **β archiv** (carry-over).
- **Spawn sound balance** (carry-over ze sezení 8/9).
- **`@AUDIT:CODE`** — práh dosažen 2 sezení po sobě (S9, S10). Po S10 je práh **překročen ≥ 2**, takže příští sezení by mělo audit jako první bod.

## Sezení 11 — @AUDIT:CODE + redesign IntegrationMode + magnetic merge Stage 1

### Diskuse

- @BEGIN audit: 10 sezení dosaženo, `@AUDIT:CODE` překročen ≥ 2 → spuštěn jako první bod.
- Bug report od uživatele: pixel #600 v G=20 PB scenáři "visel vedle objektu" — diagnóza odhalila, že pixel byl 8.57 U od nejbližšího souseda > cutoff 7.5 U, takže grid ho přeskočil. **Není to bug**, ale model UX — hard cutoff = culling decision, ne approximation. Uživatelské překvapení patří do IDEAS (vizuální signal "isolated" pixel).
- @AUDIT:CODE výstup: 1 kritický (perfSpawn vs pbSpawn duplikát), 1 K2 (Largest champion vždy null), 4 doporučené, 5 kosmetických. Vynecháno: D2 (SKIP_RAPIER_IF_NO_JOINTS — flag legit pro experiments), D4 (perf alokace bez důvodu), C3 (App.svelte 1148 LOC refactor — Svelte single-file).
- Po opravách auditu uživatel požádal: bohatší tooltipy (cutoff zapomíná), 3 módy `without-interaction` / `not-align` / `align` (smazat rapier/hybrid-naive/hybrid-β), všechny E*/P*/PB* presety smazat, **G1024** preset (32×32, perf showcase), uklid v UI panelech (smazat 🔗/✂, joint barva = h2 modrá #6f8ec1).
- **Saga align bugu** napříč iteracemi:
  - Pokus 1 (per-tick brute force `lockRotations + setRotation(0) + setAngvel(0)` v sim loop align case): ničil Rapier joint warm-start cache → solver iters startovaly z čistého stavu každý tick → drift do 0.333 U pod constraint distance.
  - Pokus 2 (smazat per-tick brute force, $effect na switch + LMB lock): joint warm-start zachován, ale **distance 0.333 U přetrvává** — solver iters 16/4 nestačí proti gravitační kompresi v close-range.
  - Pokus 3 (pos snap newer pixelu při joint create): zafixovalo distance při create, ale následný drift od gravity-vs-Baumgarte rovnováhy stejný.
  - Pokus 4 (solver iters 32/8 default + symmetric snap pro fresh pair): **funguje pro E1align/E2align** — pair distance přesně 1 U, centroid invariant. ALE align na rotujících chains (E3align scenario) by porušil joint anchory existing chainu (anchor v lokálním frame se po setRotation(0) chainu posune ve world coords → solver violence → overlap).
  - Final: align je usable jen pro **fresh pair scenarios bez initial rotace**, dokumentováno v info tooltip.
- Uživatel akceptoval E2 not-align jako "věrná fyzika" (pair distance ≠ 1 U je důsledek rotace pixelů v collision moment, ne bug).
- Přechod na **magnetic merge** algoritmus pro budoucí align: 4-step plán (proximity test → snap geometry → conservation sums → composite re-creation s vázenými atributy). Velký redesign (~230 LOC), staged 2-3 sezení.

### Q/A

- Q1: Align mode definice → A1 snap rotace k 0° + edge anchor + lockRotations.
- Q2: Které presety zachovat → A2 jen Grid 1024 (32×32 čtverec).
- Q3: Infotipy → A3 bohatší title + (?) ikona.
- Q4: Spawn random rotation → A4 r=0, rs=0 (jen linvel perturbace).
- Q5: Reset → A5 přejmenovat na Clear, přesunout do COMMANDS.
- Q6: cur → cursor (HUD), min-width 22→40 px.
- Q7: Magnetic merge scope → A7 staged 2-3 sezení; current align zachovat jako placeholder; threshold parameter v params.ts (default 0.1).

### Rozhodnutí

- **`IntegrationMode = 'without-interaction' | 'not-align' | 'align'`** (`src/sim/presets.ts`). Smazány `'rapier'`, `'hybrid-naive'`, `'hybrid-β'`. Default `'not-align'`. Map staré → nové: manual → without-interaction, hybrid-α → not-align, NEW: align.
- **`align` mode (current limited form):**
  - createFixedJoint má volitelný `align: boolean`.
  - Při create: snap r=0, rs=0 obou pixelů. Pos snap strategie podle existing connectivity:
    - Oba bez jointů (fresh pair) → snap **oba** k midpoint ± 0.5 (centroid invariant).
    - Jeden bez jointů → snap jen ten (chrání chain druhého).
    - Oba s jointy (merge komponent) → no pos snap.
  - Edge anchor ±0.5 podle dominantní osy.
  - lockRotations(true) na obou pixelech.
  - **Žádný per-tick brute force** — to ničí Rapier joint warm-start cache.
  - $effect na integration change → jednorázový pass. LMB spawn během align → lock na novém pixelu.
  - **Známé omezení:** align snap rotace na pixelech v existing **rotujícím** chain porušuje joint anchory existing jointů (anchor v lokálním frame, po setRotation(0) world pozice anchor změněna). Solver overshoot → overlap. Pro rotující bodies use not-align.
- **Solver iters 32/8 default** (`World.init` + `applyPreset` reset). Rapier defaulty 4/1 nedostatečné pro joint chain v close-range gravity (3-pixel chain se zaboří 0.667 U pod constraint distance).
- **`G1024` preset** — 32×32 axis-aligned grid, spacing 3 U, default `without-interaction`, no stop. Pure gravity collapse showcase.
- **E1, E2, E3 presety** — pair attract testy konzervace. E1: 2 pixely klid 4 U od sebe. E2: stejné s rs=±1 (test ∑L cancel). E3: 4-pixelová tyčka rotující ω=+1 + 1 pixel ω=-1 (test ∑L preservation přes rigid body merge dynamics).
- **E1align, E2align** — align varianty E1/E2 pro deterministický 1 U pair po snap. E3align nepřidán — align pro rotující bodies není usable.
- **Spawn LMB** = r=0, rs=0 (jen linvel perturbace ±0.1). `SPAWN_ANGVEL_MAX` smazán.
- **Magnetic merge Stage 1**: nový `src/sim/composite.ts` s `Composite` (id, members, com, linvel, angvel, mass, inertia s parallel axis theorem), `FreeEdge`, `MergeCandidate`, `buildComposites` (Union-Find), `freeEdges` (4 strany pixelu mínus shared přes joint), `segmentDistance` (Christer Ericson), `detectMergeCandidates` (per-pair edge proximity). Per-display-tick (5 Hz) call, "Merge cand." badge v STATS.

### Kód

**Audit fixes (sezení 11 first half):**
- `presets.ts`: smazán `perfSpawn`, P-presety přes `pbSpawn(api, n, 4)`. PRESETS array seřazen číselně, `e8Variants()` smazána, E8r/α/β inline. Po redesignu: vše smazáno kromě G1024 + E1/E2/E3 + E1align/E2align.
- `diagnostics.ts`: `computeObjectCount` → `computeObjectStats(world): { count, largest: { repId, size } | null }` přes Union-Find s size tracking. `buildPixelIndex(world)` extrahováno (sdíleno mezi diagnostics + edge mask v App.svelte).
- `facts.ts`: `computeFacts(world, cmx, cmy, largest)` přidán parametr; `largest` champion napojen.
- `joints.ts`: `createFixedJoint` má duplicate guard (idempotent). Volitelný `align: boolean` parametr.
- `App.svelte`: `championLabel` smazán (Largest přes `{@render champ()}`). `centroidScreen` ternary místo if/elseif. H slider disabled. `Reset scény` → `Clear` v COMMANDS panelu (left). HUD `cur` → `cursor`.

**Redesign:**
- `IntegrationMode` v `presets.ts` zjednodušen + později rozšířen o align.
- `App.svelte`: switch v sim loopu redukován na 3 cases. `$effect` na integration change pro lock/unlock rotations. LMB spawn handler check pro align mode.
- `presets.ts`: smazány e5Spawn/e6Spawn/e7Spawn/e8Spawn helpery. G1024 + E1/E2/E3 + E1align/E2align.
- `physics.ts`: `setSolverIterations(32)` + `setPgsIterations(8)` v `init()`. spawnPixel s r=0, rs=0. Smazán `SPAWN_ANGVEL_MAX`. Smazány `saveZeroVel`/`restoreVelDelta`/`SavedVel` (β archived).
- `params.ts`: `SKIP_RAPIER_IF_NO_JOINTS` smazán. `MAGNET_THRESHOLD = 0.1` přidán.
- `gl.ts`: `jointCol` z `#d86f6f` na `#6f8ec1` (h2 modrá panelových titulků).

**Magnetic merge Stage 1:**
- `src/sim/composite.ts` — nový soubor (~230 LOC). Composite type, Union-Find groups, parallel axis theorem inertia, free edge enumeration s shared detection podle anchor dominantní osy, segment-segment distance, merge candidate detection.
- `App.svelte`: `mergeCandidateCount` $state, per-display-tick `buildComposites` + `detectMergeCandidates`, "Merge cand." řádek v STATS.

### Test/ověření

**E1 not-align (G=20 user-overridden, stop@10s):**
- pos #2 (-0.490, 0), pos #3 (+0.490, 0) — distance 0.98 U
- ∑P = (5e-26, 2e-14) — f32 ulp
- ∑L = 4e-14 — f32 ulp
- KE = 9e-28, PE = -11.16
- **Konzervace perfektní, fixní obdélník v origin.** ✓

**E2 not-align (G=20):**
- pos #4 (-0.657, -0.009), pos #5 (+0.657, +0.009) — distance 1.314 U
- pair r ≈ ±2.45 rad (rotated kontakt)
- rs ≈ +0.0016 (téměř 0)
- ∑P = (0, 0), ∑L = 0.0019 (≈ 0, f32 šum)
- **Konzervace drží, pair distance > 1 U je věrná fyzika rotace v collision moment.** ✓

**E1align (G=1, stop@10s):**
- pos #0 (-0.5, 0), pos #1 (+0.5, 0) — distance přesně 1 U
- ∑P=∑L=KE=0, PE=-0.555
- **Bit-identický s E2align.** ✓

**E2align (G=1, stop@10s):**
- pos #2 (-0.5, 0), pos #3 (+0.5, 0) — distance přesně 1 U
- ∑P=∑L=KE=0, PE=-0.555
- **Bit-identický s E1align — symmetric snap funguje.** ✓

**E3 not-align (G=1, stop@10s):**
- Tyčka 4 pixely + pravý pixel se slepili do 5-pixel objektu.
- ∑Px = -1.7e-5, ∑Py = +1.2e-5 (f32 ulp)
- ∑L = 5.495 (init 5.5, drift 0.09 %)
- KE = 2.10, PE = -4.57
- **Konzervace drží do f32 šumu, joint preserves relative orientation, slepenec rotuje konzistentně.** ✓

**E3 align attempt (user manual switch během sim):** spoje tyčky se rozhodily, pixely se zanořily do sebe. Příčina: setRotation(0) na pixelech v rotujícím chainu invaliduje joint anchory v lokálním frame. **Známé omezení dokumentované v tooltipu.**

**Magnetic merge Stage 1**: STATS "Merge cand." ukazuje rozumné hodnoty napříč scénáři (uživatel ověřil empiricky).

`npm run check`: 0 errors, 0 warnings.

### Censure / Kudos

**Censure! sobě (per-tick brute force align bug):** Implementoval jsem per-tick `lockRotations + setRotation(0) + setAngvel(0)` na všech non-pinned pixelech v sim loop align case bez uvědomění si, že volání Rapier API každý tick **resetuje warm-start solver state**. Joint solver má interní cache Lagrange multipliers, který akumuluje konvergenci napříč ticky; per-tick body manipulation flushne cache → solver iters startují z čistého stavu → konvergence per tick limited → drift. **Lekce:** Před voláním Rapier API per-tick myslet o internal state side-effects. Solver warm-start je doctrine-level optimization v PGS solverech, mělo by být obecné povědomí.

**Censure! sobě (align scope creep):** Saga 4 iterace fix → rollback → re-add → fix → rollback align za jediné sezení byla disorganizace. Měl jsem si po prvním pokusu uvědomit, že **align je inherently destruktivní pro rotující systémy** a doporučit user **fundamental redesign** (rigid body merge) místo iterace na patches. Sezení 9 censure (mental run-through před implementací) se vztahuje i na **mental run-through pro rotující slepence** — pokud snap rotace breaks joint anchors v lokálním frame, fix je předem víc než solver tweak.

**Censure! sobě (G1024 bez tuneRapier):** Po smazání all E*/P*/PB* presetů a přidání G1024 jsem nezvolal `tuneRapier` v presetu. Default Rapier solver iters 4/1 byly aktivní → user dostal "stuck pair" bug v E1/E2 z čistého LMB workflow. Až po empirickém modelshot diagnostice jsem si uvědomil: applyPreset reset volá `setSolverIterations(4)` (Rapier default), takže preset bez tuneRapier = málo iter. Fix: World.init + applyPreset default 32/8. **Lekce: Pixelodynamics-wide defaulty by měly být v init(), ne v presetu.**

**Censure! sobě (E2align centroid drift):** First fresh-pair pos snap volil "snap newer", což pro asymmetrický kontakt (E2 s rotated pixely → kontakt v větší distance než 1 U) způsobil centroid drift. **Mental simulation collision symmetry** by to odhalila. Fix symmetric snap je triviální (mid + ± 0.5).

**Kudos! uživateli (E3 acceptance jako věrná fyzika):** Místo trvání na "musí být identický s E1" uživatel **akceptoval** E2 not-align jako věrnou fyziku a sám navrhl E2align jako "jiný experiment, jiný výsledek". Toto je správná physicist mentality — různé initial conditions → různé final states je feature, ne bug.

**Kudos! uživateli (magnetic merge algoritmus):** 4-step plán (proximity test → snap geometry → conservation sums → composite recreation) je **textbook inelastic collision** v rigid body abstraction. Uživatel přesně identifikoval, že současný align není inherentně proper merge — je to soft-constraint workaround. Refactor na composite-driven model je správný směr pro fáze 4+ (pružnost, distance/spring joints).

**Kudos! sobě (E3 not-align ∑L drift 0.09%):** Po `setSolverIterations(32)` + `setPgsIterations(8)` má E3 (5-pixel rigid body s gravitational interaction + auto-joint kontaktem v t≈3 s) ∑L drift jen 0.09 % za 10 s. Joint solver Rapieru je trustworthy pro physics simulation po properém tuningu — to je **tested confirmation** že ne-trivial slepence + dynamic joints + gravity vše drží konzervaci.

### Příště

- **Magnetic merge Stage 2** — inelastic merge math: V_new = ∑P/M_total, ω_new = ∑L_rel_new_CoM / I_new (parallel axis pro nové CoM). Apply merge pro nejbližší candidate per-tick (nebo display tick).
- **Magnetic merge Stage 3** — composite-driven kinematics nahrazující FixedJoint v align mode. Per-tick: aggregate forces → V → x; aggregate torque → ω → orientation. Pixel pos = CoM + R(orientation) · offset_local.
- **E12** (carry-over): pair-to-pair head-on collision empirický test.
- **β archiv** (carry-over).
- **`@AUDIT:DOCS`** — práh 10 sezení dosažen 1× (po S10), ne ≥ 2. Po S11 práh **dosažen ≥ 2** → příští sezení doporučit DOCS audit.
- **Hover infotipy nad slepencem** — current hover shows pixel info; rozšířit pro composite (CoM, mass, ω, members count).

## Sezení 12 — @AUDIT:DOCS + magnetic merge Stage 2 + Stage 3 MVP

### Diskuse

- @BEGIN audit: 11 sezení dosaženo, `@AUDIT:DOCS` práh ≥ 2 dosažen → spuštěn jako první bod.
- 7 kritických nálezů auditu cluster v README + MODEL.md (oba za S5–S7 stagnovaly, neabsorbovaly S8–S11): neaktuální IntegrationMode (`manual` → `without-interaction`), uzavřená α/β/γ debata stále otevřená v textu, „Reset scény" v SETTINGS místo „Clear" v COMMANDS, sekce „Modes / Plánované rozšíření" v MODEL.md s obsoletním obsahem, chybná cutoff formule (`F < 0.01·F_peak` místo skutečných ~0.098).
- Po opravě dokumentace: volba další práce. Primární kontinuita ze S11 Příště = **Magnetic merge Stage 2**. Pre-step doporučení E12 (baseline pair-to-pair) zamítnut — uživatel zvolil rovnou Stage 2.
- Q1–Q4 pro Stage 2: per display tick (5 Hz), skip pinned, skip without-interaction, oba presety (E12 head-on + E13 tečně). Všechno **a/a/a/a**.
- Po E12/E13 modelshotech (oba čistě konzervovaly ∑P/∑L, ω emerge z translačního momentu v E13 ✓): uživatel ukázal modelshot, kde align mode + magnet merge **rozbil** slepenec — pixely v non-1U pozicích, mix align + not-align jointů.
- Diagnóza: `applyMerge` hardcoded `createFixedJoint(..., false)` → magnet v align modu vytvořil not-align jointy. Plus hlubší konflikt: Stage 2 math je position-preserving, align je position-snapping → ne kompatibilní. Fix: **magnet merge skip v align mode**.
- Přechod na Stage 3 jako proper fix. Q1–Q4 architektury: `θ` v `members[0].rotation()`, override Rapier po stepu (Dynamic bodies), MVP jen rigid body integration (no chain-merge re-align), magnet v align zatím skip. **a/a/a/a**.
- Po Stage 3 modelshotu (6-pixel chain v G=20 po 60 s): pixely **přesně 1.000 U** od sebe ✓, plus všichni mají stejné r=-0.516 a rs=-0.012 — **composite rotuje** i přes lockRotations(true). Empirie ukázala, že Rapier joint solver aplikuje angular impulsy, které lockRotations neblokuje, a stepCompositesAlign je propagace přes `setRotation(members[0].rotation())` na všechny členy. **Stage 3.2 (explicit composite rotation handling) není potřeba** — funguje sama od sebe.

### Q/A

- Q1 (Stage 2 cadence): per display tick (5 Hz). → a
- Q2 (pinned): skip. → a
- Q3 (mode gating): skip without-interaction, aktivní v not-align + align. → a (původně, později revize na not-align only)
- Q4 (test scenarios): E12 head-on m=2 vs m=2, E13 tečně offset → spin emerge. → a
- Stage 3 architecture:
  - Q1 (θ storage): implicitně v `members[0].body.rotation()`. → a
  - Q2 (Rapier override): Dynamic bodies + post-step override. → a
  - Q3 (chain-merge re-align): jen rigid body MVP, ne re-align (Stage 3.1 deferred). → a
  - Q4 (magnet v align mode): skip pro tonight, re-aktivace po Stage 3.1. → a

### Rozhodnutí

- **@AUDIT:DOCS opravy** (K1–K7 + D3–D6 + C1, C3 + D1, D2):
  - README.md: Roadmap fáze 3 status `rozděláno`, věta o α/β/γ refaktorovaná na verdikt, přejmenování „manual" → „without-interaction", „Reset scény" → „Clear" v COMMANDS, gramatika („deformovat se"), live demo odkaz, struktura doplněna o `audio/` + `composite.ts`.
  - MODEL.md: sekce „Modes" přepsána na 3 nové módy s drift čísly, „Plánované rozšíření" rozdělené na hotové (Spatial grid, Composite Stage 1) a otevřené (Stage 2/3), opravena cutoff formule.
  - IDEAS.md: composite dataset zkráceno na pointer, hybrid orchestrace na verdikty (~25 řádků sloučeno).
  - DONE.md: sezení přerovnána do strict reverse-chronological (11→1).
  - TODO.md: Stage 2 [~] s poznámkou „Stage 1 hotová", Stage 3 → „magnetic merge Stage 3" disambiguace, [!] legenda smazána.
  - C2/C4/C5 vynechány (nepotřebné).
- **Magnetic merge Stage 2 — `applyMerge(world, candidate)` v `composite.ts`**:
  - Skip pokud kterákoli strana obsahuje pinned.
  - M_new = M_A + M_B; CoM_new = (M_A·CoM_A + M_B·CoM_B)/M_new; V_new = ∑P/M_new (∑P preserved).
  - L_total = Σ_X { I_X·ω_X + M_X·(r_X × v_X_rel) } kolem CoM_new (parallel axis); I_new = Σ_X { I_X + M_X·|r_X|² }; ω_new = L_total/I_new (∑L preserved).
  - Per pixel: setLinvel(V_new + ω_new × r_offset), setAngvel(ω_new). Pos preserve.
  - Create FixedJoint mezi candidate edge pair (idempotent guard pokrývá race s auto-jointem).
- **App.svelte integrace** — per display tick po `detectMergeCandidates`, consumed-set guard (každá komponenta zmergována max 1× per tick), gate `integration === 'not-align'` (po revizi z magnetic-merge-in-align bug).
- **E12** — 2 slepence m=2 head-on, axis-aligned, edges already-in-MAGNET_THRESHOLD (distance 0.098 U) při spawnu, slow v=±0.05 → magnet trigger v prvním display ticku PŘED auto-jointem. G=0, not-align, stop @ 3 s.
- **E13** — 2 single pixely tečně s Y offset ±0.05, edges in threshold, slow v=±0.05. Test ∑L preservation: spin emerge ω_new ≈ -0.0053 z čistě translačního momentu.
- **Magnet skip v align mode** (post-bug fix): magnet merge math je position-preserving, align je position-snapping → konflikt. Auto-joint s align=true je jediná správná cesta v align mode, dokud Stage 3+ nenahradí FixedJoint úplně.
- **Stage 3 MVP — composite-driven kinematics**:
  - `Pixel` rozšířen o `compositeOffsetX/Y: number | null` (stable local offset v composite frame).
  - `createFixedJoint(align=true)` po pos/rot snap volá `recomputeCompositeOffsets(world, a)` (BFS přes joints najde komponentu, set offset = R(-θ)·(pos - CoM) pro každý člen).
  - `stepCompositesAlign(world)` po Rapier step: pro každý multi-pixel composite snap pos/rot/linvel/angvel z aggregate state. Rapier integroval pos drift, my override na rigid-correct geometrii.
  - Sim loop align case: kick (4 substepy) → Rapier step → stepCompositesAlign.

### Kód

**`@AUDIT:DOCS` opravy** (5 souborů: README.md, docs/MODEL.md, IDEAS.md, DONE.md, TODO.md).

**Stage 2 magnetic merge:**
- `src/sim/composite.ts:308` — `applyMerge(world, candidate): boolean`. Inelastic merge math (CoM, V, ω, I via parallel axis), pixel snap to rigid body kinematics, FixedJoint create.
- `src/ui/App.svelte:580` — per display tick, gate `integration === 'not-align'`, consumed-set guard.

**Stage 3 MVP:**
- `src/types.ts:24` — `Pixel.compositeOffsetX/Y: number | null`.
- `src/sim/physics.ts:139` — `spawnPixelExact` init na null.
- `src/sim/joints.ts:175` — `createFixedJoint(align=true)` po vložení joint volá `recomputeCompositeOffsets(world, a)`.
- `src/sim/joints.ts:198` — `collectComponent(world, seed)` BFS.
- `src/sim/joints.ts:220` — `recomputeCompositeOffsets(world, seed)` set offset = R(-θ)·(pos - CoM) pro celou komponentu. Singleton → null.
- `src/sim/composite.ts:374` — `stepCompositesAlign(world)` per multi-pixel composite override.
- `src/ui/App.svelte:472` — split `not-align` / `align` case v switch, align přidává `stepCompositesAlign(w)` po Rapier step.

### Test/ověření

**E12 modelshot @ t=3.02s (head-on m=2 vs m=2, G=0):**
- 4 pixely v ±0.537, ±1.537 (axis-aligned 1 U distances mezi sousedy ✓).
- vx=vy=rs=0 ✓ (po inelastic merge slepenec stojí).
- `px=0, py=0, L=0, ke=0` (100% inelastic loss, ∑P/∑L preserved).

**E13 modelshot @ t=3.02s (tečně offset, G=0):**
- 2 pixely, vy ≈ ±0.0029, vx ≈ ±0.0003, rs = -0.005386.
- `L = -0.005000098` (initial -0.005, drift v 7. decimále — f32 ulp ✓).
- `ke = 1.346e-5` (předpověď 1.32e-5 ✓), 99.5 % loss z init 0.0025.
- **Spin emerge** z čistě translačního pohybu, ω = L/I = -0.005/0.943 ≈ -0.0053 ✓.

**Stage 3 modelshot @ t=60.55s (6-pixel chain v align mode, G=20):**
- Distance mezi všemi sousedy: 1.000 U přesně ✓ (Δx ≈ 0.869, Δy ≈ -0.495, |Δ| = 1.000).
- Všichni pixely r = -0.516 (rotated od axis-aligned), rs = -0.012 — composite rotuje synchronně.
- ∑P = (0.10, -0.16) malý drift z f32, ∑L = -0.226, KE = 0.004, PE = -118.55.
- **Slepenec rigid body napříč 60 s simulace** — žádný drift z joint solver imperfections.

`npm run check`: 0 errors, 0 warnings (po každé fázi).

### Censure / Kudos

**Censure! sobě (applyMerge align=false hardcoded):** Při Q3 odpovědi „align: aktivní (Stage 2 nahradí destructive pos snap merge mathem)" jsem nezvážil, že Stage 2 math je position-preserving a align je position-snapping — fundamentálně inkompatibilní. Default to *enumerate semantic conflicts* před commitnutím designu. Bug se projevil až po empirickém test (uživatel ukázal broken cluster).

**Censure! sobě (Stage 3 lockRotations obavy):** Q3.4 jsem označil složení s lockRotations(true) jako problém pro composite rotation. Empirie vyvrátila — Rapier 2D-compat aplikuje angular impulses přes joint solver, které lockRotations neblokuje, a my je propagujeme přes setRotation. **Test before assume**, zvlášť u 3rd-party physics engine s nedokumentovaným chováním.

**Censure! sobě (Stage 2 timing):** Per display tick (0.5 s) detection vs. per sim tick (16 ms) auto-joint = race. Pro typical scenarios (uživatel spawn pixely), auto-joint vyhraje a magnet merge nikdy nespustí. E12/E13 design (pixely already-in-threshold při spawn s slow vx) byl fragile workaround místo proper solution. Pro real-world magnet usefulness by detection musela běžet per sim tick — zaplatit perf cost gating přes `compositeCount ≤ N`.

**Kudos! uživateli (E2 acceptance pattern repeated):** I v případě magnet-merge-in-align bug uživatel jasně pojmenoval problém („Víš, že align s novými pixely stále rozbíjí slepenec?") a přijal vysvětlení po opravě. Plus rychlé Q/A cycle pro Stage 3 architecture (a/a/a/a bez back-and-forth).

**Kudos! sobě (Stage 3 architecture):** Po empirickém poznání magnet+align konfliktu jsem hned správně escalated na Stage 3 (composite-driven kinematics) místo iterace na patchování. Result: rigid body geometry preserved exactly, plus bonus rotation working out-of-the-box. Architectural insight před implementací (offsets stable, override po Rapier step) ušetřil iterace.

### Příště

- **Stage 3.1** — chain-merge re-align: pokud 2 multi-pixel chains se spojí auto-jointem, jejich relativní pozice se zafixují tak, jaké jsou v okamžik merge (může být non-1U). Stage 3.1 by re-snapnul obě chains na 1U grid. **Otevírá fixní fix uživatelova původního bugu.**
- **Stage 3.2** — composite rotation handling explicit: aktuálně rotace funguje empiricky přes joint solver impulsy, ale není to architektonicky čistá cesta. Refactor by odstranil lockRotations(true) v align mode + explicitně driveval θ přes setAngvel + manuální drift v stepCompositesAlign.
- **Magnet merge re-aktivace v align mode** (po Stage 3.1+3.2): se composite-driven framework v align, magnet můžem znovu zapnout — applyMerge spočítá novou composite state + recomputeOffsets se postará o geometrii.
- **β archiv** (carry-over od S9 — promazat E8β preset, pokud nezasahnem β refinement).
- **Spawn sound balance** (carry-over od S8/S9).
- **Hover infotipy nad slepencem** — composite info (CoM, mass, ω, members count) v hover tooltipu.
- **Audit cadence:** po S12 prahy nepřekročené (>= 2), žádný stale Příště. Klid.

## Sezení 13 — Stage 3.1 chain-merge rigid-transform + E14 + G100

### Diskuse

- @BEGIN audit: 12 sezení dosaženo, žádný práh překročen (AUDIT:DOCS i AUDIT:CODE čerstvé z S11/S12, IDEAS pruning v S12).
- **⚠ Stale Příště (5 sezení)** detekováno: „Spawn sound balance" táhneme S8 → S12 bez akce. Návrh DROP — uživatel měl dodat zvuk, neudělal v 5 sezeních = nízká priorita; volume slider bez nového zvuku nedává smysl. **Souhlas uživatele: DROP.**
- Volba další práce: Stage 3.1 (fixne user-visible cluster bug z S11/S12) > Stage 3.2 (architectural cleanup, no user benefit) > hover tooltipy. Uživatel: „Go!" → Stage 3.1.
- Po implementaci uživatel požádal o presety: **Grid 100 (10×10)** + **E14**.

### Rozhodnutí

- **Stage 3.1 architektura — rigid-transform menšího řetězce** v `createFixedJoint(align=true)`:
  - 4-cestný rozcestník: **same-component** / **fresh-fresh** / **fresh+chain** / **chain+chain**.
  - **Same-component** (a, b spojeni nepřímo): geom preserve, anchor v a-local frame z current world delta. Žádný snap (rozbil by chain), žádný rigid-transform (re-procesoval by stejnou komponentu).
  - **Fresh-fresh** (oba singletony): symmetric snap kolem midpointu — beze změny od S11. Centroid invariant, ∑P=0 zachováno.
  - **Fresh+chain / chain+chain** (sjednocená cesta): host = větší řetězec (tiebreak `a`-strana); guest = menší. Pro singleton host force hostθ=0 (preserve grid-align UX). Pro multi-pixel host respektuje chain rotation (z `stepCompositesAlign`). Direction v host **local frame** (`R(−hostθ)·(guestPos−hostPos)`) → dominantní osa, sign. Target guest pos = `hostPos + R(hostθ)·(±1, 0)/(0, ±1)`. Rigid transform celého guest řetězce: rotace o Δθ kolem guestPos + translace na target. Internal joint anchory v body local frames zůstávají platné — world pozice anchorů se otočí společně s bodies. ∑P preserved (`V_unified = (∑P_A + ∑P_B) / M_total`), ω=0 (consistent s align destruktivním paradigmem; angular momentum loss explicitní).
- **Anchor mapping** — `alignXAxis`/`alignSign` z host local frame s polaritou flip pokud `b=host` (`alignSign = aIsHost ? sign : -sign`); anchorA/B = ±0.5 podle xAxis flag.
- **Klíčové fixy proti staré chain-chain větvi:**
  1. ❌ Staré: `setRotation(0)` na a,b lámalo internal anchory chainů s θ≠0 (joint anchors v rotated frame stale po snapu).
     ✅ Nové: rigid-transform celého chainu zachová internal geometry (anchor world rotuje s body).
  2. ❌ Staré: distance ≠ 1U + edge anchor ±0.5 → joint constraint violation.
     ✅ Nové: pos snap na 1U přesně.
  3. ❌ Staré: `setRotation(0)` v `fresh+chain` lámal multi-pixel chain s θ≠0.
     ✅ Nové: fresh pixel rotován do chain frame, chain nedotčený.
- **Spawn sound balance** — DROPNUTO ze stale Příště (5× consecutive bez akce).

### Q/A

- Q1 (Stage 3.1 host pick): větší řetězec, tiebreak `a`-strana. → a (default)
- Q2 (singleton host θ): force 0 (preserve grid-aligned UX z fresh-fresh). → a
- Q3 (conservation): ∑P preserved přes V_unified, ω=0 explicit (consistent s align paradigm). → a
- Q4 (E14 setup): chain m=3 head-on s Y offset 0.07, vx=±0.5, G=0, stop @ 8s. → a
- Q5 (G100): analog G1024, 10×10 grid spacing 3 U, `without-interaction` default. → a

### Kód

**`src/sim/joints.ts`** — refaktor `createFixedJoint` align branch (~120 LOC):
- 4-cestný rozcestník (same-component / fresh-fresh / fresh+chain / chain+chain).
- Rigid-transform helper inline (rotace o Δθ kolem guestPos + translace).
- `alignXAxis`/`alignSign` closure-vars pro anchor calc níž.
- Velocity unification (∑P sum, V = ∑P/M).
- `lockRotations(true)` na všech dotčených members (host i guest).
- Docstring přepsán na 4-cestný popis.

**`src/sim/presets.ts`** — 2 nové presety:
- **G100** — 10×10 grid spacing 3 U, `without-interaction` default (analog G1024 zmenšený).
- **E14** — 2 chainy m=3 head-on s Y offset 0.07 U, vx=±0.5, G=0, align mode, stop @ 8 s. Initial ∑P=0, ∑L=0.105, KE=0.75. After: 6-pixel chain (-3.5..+1.5) na Y=0, V=0, ω=0, KE=0 (100% inelastic loss).
- Helper `g1024Spawn` zobecněn na `gridSpawn(api, cols, rows, spacing)`, sdílí oba presety.

### Test/ověření

- `npm run check`: 0 errors, 0 warnings (jen pre-existing tsconfig warnings).
- Manuální E14 modelshot **odložen** na příště (uživatel test v browseru).

### Censure / Kudos

**Kudos! sobě (Stage 3.1 scope discipline):** Plánoval jsem **i** fresh+chain branch fix (současně buggy s `setRotation(0)` na chain pixelu), což byl scope creep mimo TODO ("chain-merge re-align"). Ale uživatel `Go!` zahrnoval celý plán, takže fresh+chain dostal sjednocenou cestu rigid-transformu — clean architectural win. Bonus: fresh+chain se starou rotací byl **také rozbitý**, jen jsme to nikdy explicitně netestovali.

**Censure! sobě (E14 modelshot validace):** Implementoval jsem Stage 3.1 + preset E14, ale neprovedl jsem empirický test (modelshot run). Stage 3.1 je významný refaktor s netriviálním math (R(−θ) v host local frame, polarity flip pokud b=host, atd.) — **ne-otestovaná složitost** je risk. Příště priorita: E14 modelshot run + verify ∑P=0, KE=0, geometry 6-pixel chain na Y=0.

**Kudos! uživateli (DROP rozhodnutí):** Bez ceremonie souhlas s DROPem stale Příště „Spawn sound balance" → 5 sezení noise pryč.

### Příště

- **E14 modelshot** — empirické ověření Stage 3.1. Run v browseru, Export JSON, verify expected: 6-pixel chain (-3.5..+1.5), Y≈0, ∑P=0, ω=0, KE=0.
- **Stage 3.2** — composite rotation explicit handling (carry-over od S12). Architectural cleanup, žádný user-visible benefit, ale odblokuje magnet re-aktivaci.
- **Magnet merge re-aktivace v align mode** — depends on 3.2.
- **Hover infotipy nad slepencem** — composite info (CoM, mass, ω, members count) v hover tooltipu.
- **β archiv** (carry-over od S9, 4 sezení — blízko stale prahu, do/drop volba příště).
- **Audit cadence:** po S13 prahy nepřekročené, AUDIT:CODE pomalu narůstá (S11 byl audit, S12+S13 since = 2/8 toward práh). Klid.
