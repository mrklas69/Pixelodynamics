# 2026-05-07

## Sezení 1 — Definice projektu, scaffold

### Diskuse

- Definice projektu: webová sandbox simulace dynamiky pixelů.
- Cíl: experiment a zábava, žádný fixní deadline ani feature scope.
- Konvence sezení: otázky a odpovědi se číslují `Q1, Q2…` / `A1, A2…`.
- Mód spolupráce: maximální skepticismus a kriticismus ze strany AI.
- Zatím bez `GLOSSARY.md` — pojmy se postupně budují, glosář vznikne, až bude třeba.

### Rozhodnutí

**Stack:**
- Vite + TypeScript (strict, `noUncheckedIndexedAccess`)
- Svelte 5 (UI panely, runes)
- Rapier 2D (`@dimforge/rapier2d-compat` 0.19.3) — rigid body fyzika
- WebGL2 + instanced quads (raw, bez render libs)
- gl-matrix (jediná math závislost)

**Klíčový pivot:** uživatel správně poukázal, že potřebujeme FW na 2D kolize. Původní návrh (custom XPBD + shape matching) byl over-engineered. Místo toho:
- Každý pixel = 1 rigid body s nativní rotací.
- Slepování = `FixedJoint` (později breakable).
- Pružnost = `DistanceJoint` se spring koeficientem.
- Rozbití = remove joint při překročení impulse threshold.

**Konvence:**
- Identifikátory anglicky, komentáře česky.
- Bez `T` prefixu (Delphi konvence se neaplikuje).
- `kebab-case.ts` filenames.

**Pixel struktura ve FVP:**
```ts
type Pixel = {
  id: number;
  body: RigidBody;   // Rapier drží x, y, vx, vy, r, rs
  m: number;         // konstantní 1, ale strukturně přítomné
};
```
Záměr: `m` připravený teď, abychom při fázi 4 nepřepisovali API.

### Kód

Scaffold projektu (Vite + Svelte 5 + TS), instalace deps, vytvořena adresářová struktura `src/{sim,render,input,ui}`. FVP implementace:
- `src/sim/physics.ts` — Rapier World wrapper, `spawnPixel(x, y)` s náhodnými počátečními rychlostmi a rotací.
- `src/render/gl.ts` — WebGL2 instanced renderer.
- `src/render/camera.ts` — 2D ortografická projekce, `screenToWorld` helper.
- `src/input/keyboard.ts` — set stisknutých kláves.
- `src/ui/App.svelte` — layout 3 sloupců, FPS + pixel counter, roadmap v pravém panelu.
- GitHub Actions deploy na Pages, MIT licence.

### Kudos / Censure

**Kudos! uživatel:** Včasné upozornění na potřebu kolizního FW — ušetřilo to týdny psaní vlastního shape matchingu. Skvělé řízení AI mimo over-engineering.

**Censure! sobě:** První návrh (XPBD + shape matching pro slepené clustery) byl ambiciózní v matematice tam, kde existující engine řeší problém přímočaře. Default to libraries first, custom solvers last.

## Sezení 2 — Fáze 2: gravitace + UI + pivot z Rapier integrátoru

### Diskuse

- Pixelová gravitace: párová Newton, centrální, nebo k těžišti? Uživatel zvolil **párovou**.
- Object dataset architektura — pre-computed centroid + cached state pro composite objekty (fáze 3+).
- Zkratka pro úhlovou rychlost objektu: `rs` (izomorfismus s `Pixel.rs`), ne VR/omega/W.
- Bug report: "stále se rozbíhají, gravitační prak". Diagnóza přes ∑P (klesá), ∑L (diverguje na ~10⁵) → integrátor je viník.
- Levý panel: STATS + FACTS (championi). Pravý panel: SETTINGS (G, H) + COMMANDS.
- `ε` (Plummer softening) je numerický regularizátor, ne fyzikální parametr → schováno z UI.
- Spawn linvel/angvel intervaly přesunuty do `params.ts` jako konstanty programu.

### Rozhodnutí

- **Solver pivot:** manuální symplektický Euler pro FVP, **Rapier integrátor obejit**. Rapier RigidBody zůstává jen jako kontejner stavu. Až ve fázi 3 (joints, kolize) se Rapier step() vrací — kontakty pak fyzicky brání slingshotům.
- **Důvod:** Rapier addForce + step v čistě gravitační simulaci porušoval Newton 3 a zachování úhlové hybnosti. Stabilizační kroky určené na contacts/joints negativně ovlivňovaly orbital dynamics.
- **STATS sekce:** Time `D:HH:MM:SS`, Pixels, Objects, Connections, ∑P, ∑L, FPS.
- **FACTS sekce:** Fastest, Spinniest, Most momentum, Most ang. mom., Largest, Most massive — klik na `#ID` centruje kameru.
- **Home camera tlačítko** — návrat na (0, 0, zoom=32).
- **Číselné defaulty:** `G=1.0`, `H=1.0` (placeholder), `GRAVITY_EPSILON=1.5`, `GRAVITY_SUBSTEPS=4`, `SPAWN_LINVEL_MAX=0.1`.

### Kód

- `src/sim/gravity.ts` — kompletně přepsáno na `stepGravity()`: cache state do Float64Arrays, výpočet sil O(N²), kick-drift symplektický Euler, write back do RigidBody.
- `src/sim/diagnostics.ts` — total ∑P, ∑L, KE, těžiště. PE vrací `stepGravity` přímo.
- `src/sim/facts.ts` — championi, O(N) per tick.
- `src/sim/params.ts` — programové konstanty.
- `src/sim/physics.ts` — `spawnPixel(x, y)` zjednodušeno (params interní), přidáno `clear()` pro reset.
- `src/ui/App.svelte` — layout přepracován: STATS+FACTS na levo, SETTINGS+COMMANDS na pravo. ID jako klikací linky (kamera follow).
- `index.html` — title + locale `cs`.
- `vite.config.ts` — base path přepínaný env `GITHUB_PAGES`.
- `.github/workflows/deploy.yml` — auto deploy na Pages na push do main.

### Test/ověření

- ∑P, ∑L po 77 s simulace s 12 pixely: drží konstantní (oba pod 0.3 v absolutní hodnotě).
- Pixely tvoří kuželosečné dráhy kolem těžiště centrálního shluku — emergentní Keplerova dynamika.
- FPS = 60 stabilní s 12 pixely a 4 substepy.

### Kudos / Censure

**Kudos! uživateli:** Diagnostika s ∑P (klesá) a ∑L (diverguje 10⁵×) přesně lokalizovala viníka. Bez explicitních konzervačních readouts by byl bug tichý a my bychom slepě hledali jinde. Specifikace UI panelů jako konkrétní text byla extrémně užitečná — žádné rozhodování o detailech.

**Censure! sobě:** První pokus o fix (Rapier substepping) byl povrchní — předpokládal jsem truncation error, ne strukturální problém integrátoru. Mělo mi dříve dojít, že substepping pomáhá pouze symplektickému solveru, a Rapier ne nutně je. Druhá věc: zbytečně velké defaulty `G=2`, `linvel=2.0` v prvním pokusu o fázi 2 — měl jsem začít konzervativněji a postupně zvedat.

### Příště

- **Otevřená otázka pro fázi 3:** jak elegantně přepnout z manuálního integrátoru zpět na Rapier step(), aby přechod nebyl bolestný? Možná hybrid: gravitace ručně, joints + kolize Rapierem v jednom volání step po manuálním update?
- **Performance test:** kolik pixelů zvládne 60 FPS při O(N²) gravitaci + 4 substepy? Profilovat na 100, 500, 1000.
- **Vizualizace centroidu** jako křížek — uživatel to chce pro fázi 3.
- **Začít fázi 3** — slepování po straně. Designové otázky: detekce sousednosti (dotyk po straně), jeden vs. mnoho jointů na pixel, vznik composite objektu (datový vznik), update centroidu při změně topologie.
- **Pages deploy** ověřit (z konce sezení 1 stále nezaverifikováno).

## Sezení 3 — Plánování fáze 3, experimentální postup

### Diskuse

- Singularita gravitace: jakmile přijde kolizní systém + slepování přes constraint, minimální vzdálenost mezi pixely je geometricky vynucena na `r_min = U` (strana pixelu). `ε` (Plummer softening) zůstane v kódu jen jako numerická pojistka, ne jako fyzikální parametr — síla `F ∝ 1/U²` v close-encounter je konečná, ale velká, takže pojistka má smysl.
- Tři vrstvy otevřené pro fázi 3 (orchestrace Rapieru):
  1. Co Rapier `step()` interně dělá pro tělo bez external force / no joints / no contacts (gravity? damping? CCD?). Dosud spekulace, žádná čísla.
  2. Kam v cyklu zařadit `step()` — pre/post manual gravity, nebo PBD nadstavba s Rapierem jen na broadphase.
  3. Velikost driftu z kontakt-disipace vs. current float-roundoff drift z manuálního Eulera. Až bude přítomen, může být disipace dominantní a debata o symplektičnosti irelevantní.
- Postoj: postupné experimenty, každý generuje data, každý malý.
- Nápad uživatele: presety v COMMANDS panelu — tlačítko vymaže scénu, nastaví parametry, odstartuje. Volitelný auto-stop čas. Tlačítko **Export JSON** pro vložení stavu do chatu k analýze.

### Rozhodnutí

- **Singularita:** ε zůstane v kódu, ale schované jako numerická pojistka. Komentář v `params.ts` upravit (zatím TODO).
- **Pořadí experimentů:** E1 → E2 → E3 → E5.
  - **E1** Rapier baseline: 2 pixely, `gravity=(0,0)`, no joints, no contacts, manual linvel=0, 1000 steps. Měřím změny pos/linvel/angvel.
  - **E2** Rapier velocity damping: jako E1 ale s nenulovou linvel. Měřím tlumení (linearDamping default).
  - **E3** Rapier FixedJoint reakce: 2 pixely + joint, jeden kopnu, druhý reaguje. Měřím ∑P drift, joint stiffness.
  - **E5** Hybrid: manual gravity + Rapier `step()` bez jointů. Měřím ∑P/∑L drift vs. čistý manuální (sezení 2).
  - **E4** (contact response) a **E6** (close-encounter stability) → IDEAS, na pozdější sezení.
- **Architektura presetů:** experiment = preset. Žije v `src/sim/presets.ts`, UI v COMMANDS panelu, žádné samostatné `src/experiments/` ani Vitest. Export JSON = univerzální nástroj pro share stavu do chatu.

### Kód

**Preset/experiment infrastruktura:**
- `src/sim/presets.ts` — typy `Preset`, `PresetAPI`, `IntegrationMode`, `Modelshot`. Pole `PRESETS` s E1, E2, E3, E5. Funkce `buildModelshot()`.
- `src/sim/physics.ts` — `spawnPixelExact(x, y, vx, vy, r, rs, m)` pro deterministický spawn. Stávající `spawnPixel()` (random pro LMB) zachován jako tenký wrapper.
- `src/ui/App.svelte`:
  - Stav `paused`, `integration`, `currentPreset`, `stopAtTime`, `toast`.
  - Mode-aware loop: `manual` (stávající Euler), `rapier` (jen `world.step`), `hybrid` (oboje). Výchozí mód `manual` — LMB chování fáze 2 zachováno.
  - `Space` keydown = toggle pause; pauznutá simulace zmrazí čas, ale kamera + UI dál fungují.
  - Auto-stop: dosažení `preset.stopAtTime` → pauza + auto-export modelshotu do clipboardu.
  - Tlačítka **⏸ Pause**, **📋 Export JSON**, sekce **PRESETS** s aktivním zvýrazněním.
  - Toast notifikace (info "modelshot zkopírován" apod.).
  - `Reset scény` rozšířen o reset preset stavu.

**Co E3 NEDĚLÁ (vytknuto v komentáři presetu):** FixedJoint API ještě neexistuje (fáze 3 work). E3 je zatím pouze geometrický baseline (2 pixely u sebe, jeden v pohybu); reálné měření joint reakce vyžaduje doimplementovat joint API.

### Test/ověření

**E1 — Rapier baseline: PASSED (bit-identical).**
2 pixely v (-5, 0) a (+5, 0), vše nuly. Run 10 s, `rapier` mód, gravity=(0,0). Výsledek pos / linvel / angvel / r **bit-identical** k inputu. Rapier `step()` na klidu nepřidává žádný jitter (CCD, sleep, broadphase OK).

**E2 — Rapier velocity damping: PASSED, default damping = 0.**
2 pixely, jeden s `linvel=(1, 0)`, druhý s `angvel=0.5`. Po 10 s: `linvel` i `angvel` **bit-identical** se začátkem. Rapier `linearDamping` i `angularDamping` defaultně 0 — nic nemusíme vypínat.

**Boční nálezy z E2 (před opravou loopu):**
- `simTime` se akumuloval z `realDt` (wall-clock), ale Rapier integroval pevný `FIXED_DT = 1/60`. Drift ~0.3% mezi sim a Rapier časem. **Opraveno** Glenn Fiedler accumulatorem (`simTime += FIXED_DT` per fixed step). Po opravě `simTime = 600 × 1/60 ± float roundoff`.
- `r` (rotace) se v Rapieru **wrappuje do `(-π, π]`**. Pro reconstrukci „kolik otoček" by byl potřeba kumulativní counter, který zatím nemáme.
- **f32 drift v Rapieru:** `pixel.x` po 10 s s `vx=1` skončil na `4.99993` místo přesně `5.0`. Cumulative ~1e-7/krok × 600 = 6e-5. Není bug — `@dimforge/rapier2d-compat` používá f32 interně. **Pro E5 to znamená, že drift ∑P/∑L ≤ 1e-4 je „f32 floor", ne strukturální problém.**

**E5 vs. E5m — naive hybrid je BROKEN.**

Setup: 12 pixelů v D4 symetrii, G=1.0, ε=1.5, 60 s. E5 hybrid (manual + Rapier step), E5m manual only.

Konzervace (oba běhy):
- ∑P, ∑L: E5m drift ~1e-15/-1e-16 (**f64 epsilon**), E5 drift ~1e-12 (**f32 epsilon**). Hybrid je o 10⁴ worse jen kvůli tomu, že stav prochází Rapier WASM bridge ve f32.
- KE: 2.65 (manual) vs 2.68 (hybrid) — téměř shodné v okamžiku. Ne diskriminační (D4 symetrie).

Diskriminační nález: **inner pixely (start radius 1.5√2)** v manual módu **nepřešly přes počátek** (#8: (-1.5, -1.5) → (-1.36, -1.36), r = 1.92). V hybrid módu **přešly do opačného kvadrantu** (#8: (-1.5, -1.5) → (+2.06, +2.06)).

Hybrid prošel **víc oscilací** než manual za stejný `simTime`. **Confirmed**: naivní `manuální stepGravity + Rapier step()` integruje pos dvakrát (manuální drift + Rapier drift), simulace běží efektivně násobně rychleji.

### Rozhodnutí (konec sezení)

- **Architektura fáze 3 — odloženo** dokud nebudou jointy/kontakty implementované. Naive hybrid je nepoužitelný, ale konkrétní volba mezi (α) velocity-Verlet split, (β) save-zero-restore vel, (γ) Rapier step jen když jsou jointy/kontakty — vyžaduje reálný scénář s vazbou.
- Manual mode zůstává **default** pro všechno bez kontaktů. ∑P/∑L drift na f64 floor.
- Tři architektonické varianty (α/β/γ) zaznamenány do IDEAS.

### Performance test — naivní O(N²) gravita, manual mód, G=1

| N | Páry | FPS | Vyhodnocení |
|---|---|---|---|
| 100 | 4 950 | 60 | strop neviditelný |
| 500 | 124 750 | 60 | strop neviditelný |
| 1000 | 499 500 | 45 | viditelný frame drop |
| 2000 | 1 999 000 | 12 | nepoužitelné |

**Strop O(N²) padá kolem N≈700.** P1000→P2000 přechod je 4× víc párů, 3.75× pomalejší → **čistá O(N²) škála**, ~10 ns/op (rozumné pro V8 hot loop).

**Cíl ze sezení 1 „1000 pixelů @ 60 FPS"** je s naivní gravitací **nesplnitelný**. Spatial grid (uniform + cutoff) by měl dát ~10× speedup → 1000 @ 60 FPS s rezervou.

### Rozhodnutí (konec sezení)

- **Spatial grid promoted na prioritu [!]** — viz TODO.md. Bez něj je fáze 3 work limitovaná na N ≤ 500.
- Hybrid orchestrace pro fázi 3 (α/β/γ) zaznamenána v IDEAS, rozhodnutí odloženo na reálný joint scénář.

### Kudos / Censure

**Kudos! uživateli:** Návrh preset systému + JSON export do chatu byl přesně to, co experimenty potřebovaly — bez něj bych dělal ad-hoc šahárny v dev konzoli a numerická data bychom interpretovali z hlavy. Plus dobrá disciplína u Q/A — nestávalo se, že bych získal nejednoznačnou odpověď.

**Censure! sobě:** Predikce „naive hybrid odletí 2× rychleji" byla správná v mechanismu, ale špatná v očekávaných diagnostických signálech — nevěděl jsem, že D4 symetrie initial config zruší ∑P i ∑L automaticky a hybrid bude na konzervaci vypadat OK. Měl jsem hned navrhnout ASYMETRICKÝ test setup, ne D4. Plus jsem v jedné iteraci nepředvídal clipboard issue s auto-stop bez user gesture — fallback dialog jsem měl mít od začátku.

### Příště

- Rozhodnout se mezi: **(a) Spatial grid** (uniform + cutoff), **(b) FixedJoint API + E3** (otevírá fázi 3 a test α/β/γ), **(c) Centroid kříž + Pages deploy** (kosmetické dluhy).
- Implementační poznámka pro spatial grid: cells size ≈ cutoff distance; pro G·m·m/(r²+ε²)^1.5 cutoff cca 5-8 U (force pod 1% peak).
- Až přijde fáze 3 reálně: testovat (α/β/γ) na minimálním scénáři 2 pixely + FixedJoint, jeden kopnu — porovnat trajektorie s analytickým řešením rigid two-body.

## Sezení 4 — UI polish, camera lock, spatial grid, dokumentace

### Diskuse

- Uživatel přinesl seznam 7 úkolů: patička, pixel-as-rámeček, hover infotipy, FACTS klik bug, camera lock + status, 4 sigfigs ve STATS, README odborněji.
- Triáž: 4 (bug) → 6 → 1 → 5 → 2 → 3 → 7 → spatial grid.
- Q1: Volba mezi (a)/(b)/(c) z minulého „Příště" — uživatel zvolil seznam + spatial grid jako poslední bod.
- Q2: Patička obsah — `Pixelodynamics — sandboxová simulace pixelové dynamiky` / `(c) 2026 mrklas69 · MIT · github.com/…`. Plovoucí pod celým layoutem.
- Q3: Šířka rámečku → hybrid `max(0.05 U, 1 px)`.
- Q4: Hover tooltip plovoucí u kurzoru.
- Q5: Bug FACTS — Fastest pixel typicky odlétá slingshotem; jednorázové centrování ho jen vytlačí ven z viewu. Lock kamery problém eliminuje strukturálně.
- Q6: Lock unlock — WASD pohyb (implicit) **a** Esc.
- Q7: README + MODEL.md (varianta c) — README středně odborné, MODEL.md plně formální.

### Rozhodnutí

- **Camera state** se přesunula z plain objektu do `$state(createCamera())` proxy, aby HUD reaktivně reagoval na změny pos/zoom/lock z render loopu. Per-frame property reads přes Proxy přes celou aplikaci jsou v praxi neměřitelné na 60 FPS s 1000 pixely.
- **Lock follow** v render loopu, *po* simulačním kroku — pixel je v aktuální pozici, kamera ho přesně kopíruje. Pixel zmizel ze světa → zámek se shodí + toast.
- **Pixel rámeček** přes fragment shader `discard`, ne přes line geometry — jeden draw call zachován, žádná multiplicita vertex bufferu.
- **Hover hit-test** se inverzí rotace zaměřuje pixel přesně, takže kliknutí v rohu rotujícího pixelu funguje správně.
- **`fmtSig4`** centralizovaný v `src/ui/format.ts`, `Number(n.toPrecision(4)).toString()` — odstraní trailing nuly. Pro extrémní hodnoty (~1e-15 ∑P drift) zůstane exponentiální zápis, který je v tomto kontextu žádoucí.
- **Spatial grid** s hard cutoff bez smoothing tail. Trade-off: ∑P/∑L exact, KE má drobné skoky. Pro orbital scénář (klastry, ne homogenní plyn) zanedbatelné. Smoothstep tail v IDEAS jako opt-in dluh.
- **Naive O(N²)** zachován jako přepínatelný baseline (`GRAVITY_USE_GRID = false`) pro budoucí porovnávací experiment.

### Kód

**Camera + lock + HUD (`src/ui/App.svelte`, `src/types.ts`, `src/render/camera.ts`):**
- `Camera.lockTargetId: number | null` přidáno do typu, default null.
- `lockCameraOn(id)`, `unlockCamera()`, `homeCamera()` (s unlock).
- Render loop: po sim kroku, pokud `lockTargetId !== null`, `find` pixel a posuň. Jinak unlock + toast.
- `onKeyDown`: `Escape` shodí zámek; `Space` zachované pause.
- WASD detekce: jakýkoliv `panned = true` → unlock.
- HUD `<div class="hud">` přes canvas, `position: absolute, bottom-left`.

**4 sigfigs (`src/ui/format.ts`, `App.svelte`):**
- Helper `fmtSig4(n: number): string`.
- Aplikováno: ∑P, ∑L, hover tooltip, HUD x/y/zoom, championship hodnota.

**Patička:**
- Grid layout z `1fr` na `1fr / auto`.
- `<footer class="footer">` s grid-row 2, grid-column `1 / -1`.
- `<h1>Pixelodynamics</h1>` smazán z levého panelu; `panel > h2:first-child` margin-top 0 aby panely seděly na vrch.

**Pixel rámeček (`src/render/gl.ts`):**
- VS pásuje `out vec2 v_local` (-0.5..0.5).
- FS: `vec2 d = vec2(0.5) - abs(v_local); if (min(d.x,d.y) > u_borderHalfWidth) discard;`
- Uniform `u_borderHalfWidth`, render() signature rozšířen o parametr.
- App.svelte počítá `Math.max(0.05, 1 / camera.zoom)` per frame.

**Hover (`App.svelte`):**
- `pickPixel(wx, wy)` — O(N) AABB test po inverzní rotaci, threshold 0.5 U.
- `pointermove`/`pointerleave` na canvas.
- `hover: $state` s pos + atributy + screen pozice. Tooltip CSS `position: fixed`.

**README + MODEL.md:**
- README: tabulka stacku, conservation tabulka, perf tabulka, ovládání tabulka, struktura adresářů.
- MODEL.md: jednotky, stav pixelu, moment setrvačnosti čtverce, Newton+Plummer kernel, PE souhlasná s force, symplektický Euler kick-drift, substepping, fixed timestep accumulator, modes (manual/rapier/hybrid), composite object preview, spatial grid theorie, references.

**Spatial grid (`src/sim/gravity.ts`, `params.ts`):**
- `GRAVITY_USE_GRID = true`, `GRAVITY_CUTOFF_FACTOR = 5.0` v `params.ts`.
- `accumulateForcesNaive` — extrahováno z původního kernelu.
- `accumulateForcesGrid` — uniform grid, cell = cutoff = `5·ε`.
- Klíčování: `cy * STRIDE + cx` po offsetu, `STRIDE = 65536`. Pixel mimo range clipne na edge buňku.
- Module-level `gridBuckets: Map<number, number[]>` + `bucketPool: number[][]` — recyklace polí proti GC.
- `clearGrid()` na začátku každého `accumulateForcesGrid`.
- Iterace: pro každou neprázdnou buňku × 3×3 sousedství × pixel páry s `j > i`. Hard cutoff `r² > cutoff²` → skip.
- Konzervace argumentace: párový kernel s Newton 3 → ∑P exact bez ohledu na cutoff. Radiální → ∑L exact. PE je sumovaná v rámci páru → konzistentní s force.

### Test/ověření

- `npm run check` (svelte-check + tsc) — 0 errors / 0 warnings.
- `npm run build` — produkční bundle prošel, 1.75 MB / 660 kB gzip (Rapier WASM dominuje).
- Empirická validace spatial gridu (∑P/∑L drift, FPS) odložena na sezení 5 — vyžaduje srovnávací run E5 vs. E5g (12 pixelů, 60 s, naive vs. grid).

### Kudos / Censure

**Kudos! uživateli:** Klíčová intuice u bodu 4 — místo `centerOnPixel(id)` jednorázového skoku navrhl rovnou *lock follow*, čímž odhalil, že bug není ve `find`, ale v UX modelu (Fastest pixel se hned po skoku přesune jinam). Strukturální fix místo plástru.

**Censure! sobě:** Predikce FACTS bug jako `find` selhání byla povrchní. Kdybych si přečetl `centerOnPixel` se skepsí, došlo by mi, že i když `find` najde pixel, kamera ho jen jednorázově centruje a další frame on uteče — efekt „černé obrazovky" je důsledkem toho, že rychlý slingshot pixel je daleko od ostatních a viewport ukazuje prázdný prostor. Default to *behaviorální* analýzu, ne lookup analýzu, když uživatel říká „nefunguje".

**Censure! sobě 2:** Spatial grid s hard cutoff jsem hodil bez empirického ověření KE driftu. Měl jsem nejdřív specifikovat, jaký drift je akceptovatelný, a pak rozhodnout, jestli smoothing potřebuje být v draftu. Místo toho jsem to odhodil jako „dluh" — což je zbabělý KISS.

### Post-mortem: skrytý projection bug odhalený camera lockem

Uživatel spustil `applyPreset('e1')` (nebo podobný malý setup), klikl Spinniest link na pixel #0 (pos (0.387, 0.918), |rs|=0.55), HUD ukázal `Lock #0 · x 0.3875 · y 0.9183`, ale viewport zacentroval na world (-2, -12). Tooltip nad pixely se posouval „nahoru a doprava" oproti vykreslené pozici.

**Diagnóza:** v `src/render/camera.ts` byla projekční matice skládána

```
mat3.fromTranslation(m, [-cam.x, -cam.y]);
mat3.scale(m, m, [sx, sy]);
```

V gl-matrix je `mat3.scale(out, a, v)` `out = a × S`. Po těchto dvou řádcích matice odpovídá `M = T(-cam) × S`, což aplikováno na vector dává `NDC = sx·world.x − cam.x` — tj. translation je v post-scale (NDC) prostoru, ne ve world. Měla to být `M = S × T(-cam)`, dávající `NDC = sx·(world.x − cam.x)`.

**Proč to nepadlo dřív:** sezení 1–3 testovala výhradně s `cam = (0, 0)` (Home camera, presety s pozicemi okolo počátku, `homeCamera()` po každém resetu). Pro nulovou kameru se rozdíl mezi `sx·world − cam` a `sx·(world − cam)` smazává.

`screenToWorld` byl počítán algebraicky správně pro *zamýšlenou* projekci (`world = (px − W/2)/zoom + cam`), takže s rozbitou render maticí se hover hit-test rozcházel s viděným pixelem o `cam · (1 − W/(2·zoom))` — což je škála ~30× pro W=1920, zoom=32. Odtud „nad pixelem, ale daleko".

**Fix:** start na scaling, přidat translate zprava:

```ts
mat3.fromScaling(m, [sx, sy]);
mat3.translate(m, m, [-cam.x, -cam.y]);
```

`mat3.translate(out, a, v)` je `out = a × T(v)`, takže `S × T(-cam)` jak chceme. Ověřeno algebraicky: výsledná matice column-major má `out[6] = -sx·cam.x`, `out[7] = -sy·cam.y`, `out[0]=sx`, `out[4]=sy`. Aplikováno: `NDC = sx·(world − cam)`. ✓

**Censure! sobě:** matice gl-matrix má jasně documentaci (`mat3.scale(out, a, v): out = a × S`), ale autor (já v sezení 1) si přidal poznámku „Skládáme: M = S * T" na řádku, která je chybně — kód reálně dělá M = T × S. Bug nebyl detekován testy, protože FVP scénář používal jen cam=0. Default to **algebraické ověření na nenulovém vstupu** u každého matice-skládacího kódu, ne na (0,0) případu, kde se vše krátí.

### Saga: Preset E6 — Lagrange L1 (tři kola fixů)

**Záměr:** ověřit gravitační model na statickém L1 setupu — cluster (30 ks, m=30 total) vlevo, singlet (m=1) vpravo na 20 U, test pixel na L1 mezi nimi. Pokud správné, test by neměl driftovat víc než 1 px za minutu.

**Iterace 1 (preset bez pinningu):** Spawn 30 free pixelů na (-10,0), 1 free na (+10,0), test na (5.288, 0). Po 65 s test pixel uháněl s vx=-1.67, x=-2.36 (cyklic mezi cluster a singlet). Cluster i singlet se mezitím posunuly. Diagnóza: **statický L1 vyžaduje fixní hmoty**, jinak binárka kollaboval. Fix: nová atribut `Pixel.pinned`, propagace přes `spawnPixelExact`, `PresetAPI.spawn` rozšířen.

**Iterace 2 (pinning OK, ale stále kollaboval):** Cluster + singlet stáli, ale test pixel byl po 30 s na (-9.999, 0) — **uvnitř clusteru**. Diagnóza: bisekce konvergovala k falešnému lokálnímu minimu blízko clusteru. Důvod: **chybějící faktor `r`** ve formuli Plummer force. Plummer F per unit mass je `G·M·r/(r²+ε²)^1.5`, ne `G·M/(r²+ε²)^1.5`. Při substituci do bisekce jsem zapomněl `r` v čitateli. Fix: opravit obě strany rovnice na `M·r/(r²+ε²)^1.5`. Plus snížit `stopAtTime` na 30 s (saddle τ ≈ 4 s, ne 12.5 s — také přepočítáno).

**Iterace 3 (formule správná, bisekce ale stále trefila falešný kořen):** Po fixu predikátu test pixel na -9.999 znova. Diagnóza: **Plummer rovnováha má tři zero crossings**:
- `r ≈ ε²·M2/(M1·D²) ≈ 2.8e-4` — falešný L-bod uvnitř Plummer ε na cluster straně,
- `r ≈ 17.43` — pravý L1 (chtěný),
- blízko `r=D` — třetí falešný uvnitř ε singletu.

Plus můj predikát `if (f1 < f2) lo = r` byl obrácený oproti tomu, jaký je signum f(r) na hranicích zúženého intervalu. Klasický 1/r² má jen jeden kořen, takže intuice z toho byla zavádějící. Fix: zúžit bisekci na `[r_classical − 2ε, r_classical + 2ε]` kolem klasické hodnoty `D/(1+√(M2/M1)) = 16.91`. Predikát: `f1 > f2 → lo = r` (cluster pull dominant → posun mid k singletu).

**Výsledek po iteraci 3:** test pixel po 30 s na `7.4321746826171875` (přesně `f32(L1)`, **nepohnul se ani o 1 ulp**), `vx = 1.9e-7`, KE = 1.8e-14, ∑P = 1.9e-7. Saddle nestabilita probíhá v rychlosti, ne v pozici. Statický gravitační model verifikován.

**Censure! sobě, zásadní:** dva fixy téhož presetu = numerická data byla užitečná, tři už ne — kdyby bylo „při psaní formule sanity check `console.warn` na netriviálním vstupu" reflexem, ušetřilo by se čtení dvou modelshotů. Memory `feedback_sanity_check` přidána.

**Kudos! uživateli:** vůbec navrhl tento test. Nedůvěra v plynulost simulace = užitečný stress test, který odhalil tři skryté chyby (projection matrix bug + dvě L1 chyby), kterých by se nikdy nezbavily ani typecheck, ani sezení 1–3 manuální testy.

### Příště

- **Empirická validace spatial gridu** — preset `E5g` (stejný setup jako E5, ale `GRAVITY_USE_GRID=true`). Spustit 60 s, srovnat ∑P/∑L drift a `simTime` per frame s naive baseline. Pokud KE drift > 1 %/s, doplnit smoothstep tail.
- **Pages deploy verifikace** — dluh ze sezení 1 (otevřený přes 3 sezení).
- **Vizualizace centroidu** křížek — připravený teren (composite object dataset v IDEAS).
- **Volba mezi (b) FixedJoint API + E3 / (c) další UI dluhy** — viz minulé „Příště".
- Při fázi 3 použít edge mask 4 bits per pixel pro vynechání hrany u spoje (rámeček infrastruktura připravená, jen nový uniform/instance attribute potřeba).

## Sezení 5 — Empirická validace gridu, smoothstep tail, PE v modelshotu

### Diskuse

- @BEGIN audit našel: Pages deploy stale Příště 4 sezení v řadě, threshold ≥5 → příští sezení už stale.
- Volba (1) Spatial grid validace, jak doporučeno.
- Před kódem nálezy z četby:
  - **E5m je tichý zombie**: po sezení 4 (default `GRAVITY_USE_GRID=true`) E5m běžel s gridem místo deklarovaného naive baseline ze sezení 3. Diary popis falešný.
  - **D4 setup E5/E5m neumí grid otestovat**: max distance ≈ 8.49 U, většina párů < 6 U. Cell size = cutoff = 7.5 U → všechny relevantní páry **uvnitř cutoff** → grid kernel sumuje stejné páry jako naive → bit-identický výsledek. Test, který triviálně projde.
  - Pro validaci gridu MUSÍME mít spawn s páry > cutoff.
- Q1 (spawn): zvolen 4×3 grid se spacingem 6 U → range 18×12 U, 49/66 párů přes cutoff.
- Q2 (délka): 60 s konzistentní s E5m.
- Q3: implementace hned.
- Po implementaci E7n+E7g a obdržení modelshotů: nález **bez `pe` v modelshotu nelze přímo verifikovat ∑E**. KE samo o sobě osciluje s PE.
- Druhá nesedící čísla v `params.ts`: komentář `F(cutoff) ≈ 0.008·F_peak` byl bullshit. Přepočet: `F(r) = G·m·m·r/(r²+ε²)^1.5`, peak v `r = ε/√2 ≈ 1.06`, `F(7.5)/F_peak = (7.5/58.5^1.5) / (1.06/3.374^1.5) = 0.01676 / 0.1711 ≈ **0.098**`. Komentář počítal `(ε²/(r²+ε²))^1.5 = f(r)/f(0)` (kernel ratio bez faktoru r), což je úplně jiná veličina.
- Uživatel poté řekl „spusť server vždy jako součást @BEGIN" → projektový `CLAUDE.md` s definicí maker.

### Rozhodnutí

- **Sub-fix E5m:** explicitní `api.setUseGrid(false)` aby reprodukoval baseline ze sezení 3.
- **Presety E7n / E7g:** identický 4×3 spread spawn (12 px, spacing 6 U), `manual` integrátor. E7n = `setUseGrid(false)`, E7g = `setUseGrid(true)`. `stopAtTime = 60`.
- **Smoothstep tail:** přepnut z opt-in dluhu na default. `GRAVITY_TAIL_WIDTH = 1.0`. 3-2 polynom `W(r)`, modifikovaný potenciál `U_mod = U·W`, force odvozená rigorózně z `-dU_mod/dr`, ne ad-hoc tlumení.
- **PE v `Modelshot.diagnostics`:** cache `lastPE` v App.svelte z poslední `stepGravity` substepu. Lag jednoho SUB_DT = 1/240 s (zanedbatelný pro drift signál na 60s škále). Reset v `resetScene`.
- **Projektový `CLAUDE.md`** s makry `@BEGIN` (dev server start, audit cadence, stale Příště) a `@END` (DOCS, check, commit, stop server). Globální CLAUDE.md odkazuje na `<project>/CLAUDE.md` nebo `<project>/docs/PROMPTS.md` u menších projektů.
- **Smoothstep math (klíčové):**
  - `W(r_raw) = 1` pro `r ≤ r_inner = cutoff - tailWidth`
  - `W(r_raw) = 1 - (3t² - 2t³)` pro `r ∈ [r_inner, cutoff]`, `t = (r - r_inner)/tailWidth`
  - `W(r_raw) = 0` pro `r ≥ cutoff` (skip)
  - `dW/dt = -6t(1-t)`, `W' = dW/dr_raw = -6t(1-t)/tailWidth`
  - `F_x = -dU_mod/dr_raw · dx/r_raw = G·m·m · [W·invR3 + 6t(1-t)/(tailWidth·r_soft·r_raw)] · dx`
  - `pe -= G·m·m · W / r_soft`
  - **Sanity check t=0**: W=1, extra=0 → standardní Plummer. **t=1**: W=0, extra=6·1·0=0 → force=0. Spojitost na obou koncích ✓.

### Kód

- `src/sim/presets.ts`:
  - E5m doplněn `api.setUseGrid(false)` + komentář k tiché regresi.
  - **E7n** (`naive baseline`) a **E7g** (`spatial grid` validace), oba 60 s.
  - Helper `e7Spawn()` — 4×3 grid, spacing 6 U.
  - `Modelshot.diagnostics` rozšířena o `pe: number`.
  - `buildModelshot()` signature rozšířena o `pe` v diag parametru.
- `src/sim/gravity.ts`:
  - Import `GRAVITY_TAIL_WIDTH` z params.
  - `accumulateForcesGrid` — uvnitř pair loopu detekce transition zone (`r2raw > innerCutoff2 && tailWidth > 0`), výpočet W a extraTerm, aplikace na force a PE.
  - Komentář kernelu rozšířen o popis konzervací s window function.
- `src/sim/params.ts`:
  - Komentář `GRAVITY_CUTOFF_FACTOR` přepsán: skutečné `F(cutoff)/F_peak ≈ 0.098`, ne 0.008. Doplněn závěr ze sezení 5: hard cutoff je culling decision, ne approximation.
  - **Nová konstanta** `GRAVITY_TAIL_WIDTH = 1.0` (1 U transition na vnitřním okraji cutoffu).
- `src/ui/App.svelte`:
  - Cache `let lastPE = 0`.
  - V gravity substep loopu `lastPE = stepGravity(...).pe`.
  - `exportModelshot` propaguje `lastPE` do `buildModelshot` jako `diag.pe`.
  - `resetScene` resetuje `lastPE = 0`.
- **Projektový `CLAUDE.md`** (root) — definice maker `@BEGIN` a `@END`.

### Test/ověření

`npm run check` — 0 errors, 0 warnings. Empirické runs:

**E7n (naive baseline):**

Manuální výpočet PE(0) (12 pixelů at rest, ε²=2.25, G=1, m=1):

| r² | r_raw | r_soft = √(r²+2.25) | počet párů | příspěvek 1/r_soft |
|---|---|---|---|---|
| 36 | 6 | 6.185 | 17 | 2.749 |
| 72 | 8.485 | 8.617 | 12 | 1.392 |
| 144 | 12 | 12.094 | 10 | 0.827 |
| 180 | 13.416 | 13.500 | 14 | 1.037 |
| 288 | 16.97 | 17.037 | 4 | 0.235 |
| 324 | 18 | 18.062 | 3 | 0.166 |
| 360 | 18.97 | 19.033 | 4 | 0.210 |
| 468 | 21.63 | 21.685 | 2 | 0.092 |

Σ = 6.708. **PE(0) = -6.708. E(0) = 0 + (-6.708) = -6.708.**

E7n modelshot @ t=60: KE = 3.7666, PE = -10.4753 → **E(60) = -6.7087**.

**∑E drift: 6e-4 (1 část v 10⁴) za 60 s** ≈ truncation symplectic Eulera s SUB_DT=1/240 s. ✓

**E7g (grid + smoothstep):**

Pro initial state s window: jen páry s `r_raw < r_inner = 6.5` přispívají plně. Páry s `r_raw=6` (17 ks) → W=1. Páry `r²=72, r_raw≈8.49` → mimo cutoff (skip). Vyšší r² stejně mimo.

**PE(0) = -2.749. E(0) = -2.749.**

E7g modelshot @ t=60: KE = 3.7339, PE = -6.4839 → **E(60) = -2.7500**.

**∑E drift: 1e-3 za 60 s** — stejný řád jako naive. ✓ Smoothstep konzistentní s integrátorem.

**Konzervace ∑P, ∑L:** oba běhy `px = 0, py = 0, L = 0` (D2 symetrie zachovaná do roundoffu, např. E7g `py = -2.78e-17`).

**Pixel divergence E7n vs. E7g:**

| | E7n (naive) | E7g (grid + smooth) |
|---|---|---|
| Pixely v radiu | 4-10 U | **24-28 U** (!) |
| KE @ t=60 | 3.7666 | 3.7339 |
| PE @ t=60 | -10.4753 | -6.4839 |
| E_total | -6.7087 | -2.7500 |
| Vnitřní páry | rozdělené | sklesly do binárek (Δ ≈ 3.7 U) |

Dynamicky úplně jiný systém. E7g má **méně negativní E_initial** (jen 17 vs. 66 párů přispělo) → méně vázaný systém → větší rozlet. Pixely tvoří binárky (např. 0+1) které pak orbitují jako rigid pairs.

### Censure! sobě

**1. F(cutoff) ≈ 0.008 byl bullshit**: ze sezení 4 zděděný komentář v params.ts. Vznikl z `(ε²/(r²+ε²))^1.5` = f(r)/f(0) (kernel ratio bez faktoru `r`). Sanity check `F(7.5)/F_peak` numericky v hlavě by ihned ukázal ~0.1 → nesedí. **Memory `feedback_sanity_check` se aplikuje i na komentáře a docs**, ne jen kód.

**2. D4 setup pro grid validaci**: sezení 3 cenzurovalo D4 jako špatný experimentální setup → mělo to být v paměti. Pak v sezení 4 jsem grid validoval mentálně proti E5/E5m (D4) místo aby spawn měl páry > cutoff. Validační design je oddělené řemeslo od implementace.

**3. Tichá regrese E5m**: po sezení 4 jsem přepnul `GRAVITY_USE_GRID` default na `true`, ale neaktualizoval E5m. Diary popsal E5m jako naive baseline ze sezení 3, kód ale běžel grid. **Refactory mají sanity check podobně jako matice — rerun starých presetů po změně defaultů.**

**4. Architektonická chyba — předpoklad smoothstep ≈ approximation**: implicitně jsem předpokládal, že smoothstep tail přiblíží E7g k E7n. Smoothstep řeší **energy conservation across cutoff** (KE skoky), NE **approximation quality** (cut long-range gravity zůstává cut). To jsou ortogonální problémy. Měl jsem to oddělit dříve a explicitně se ptát: jaký je use case? Pro Pixelodynamics (klastry slepené pixely) je culling OK — relevantní páry jsou close-range. Pro „rozprostřený plyn" by bylo třeba větší `CUTOFF_FACTOR`, ne lepší tail.

**Kudos! uživateli:** spustil jak E7n, tak E7g s pe polem hned po implementaci. Bez potvrzeného PE od integrátoru bych v hlavě počítal jen KE a tipoval zdroje driftu. Plus manuální výpočet PE(0)=-6.708 z 66 párů × 1/√(r²+ε²) bylo **diskriminační** — kdyby sedělo s jiným číslem než modelshot E(60), bylo by to první místo kde hledat bug v naive kernelu nebo v sumarizační logice.

**Kudos! uživateli (workflow):** požadavek „spusť server vždy jako součást @BEGIN" je systémová opt — místo opakovaného „startoval jsi server?" v každé session se to automatizuje. Okamžité založení projektového CLAUDE.md s definicí makra je správnější než save-feedback-memory; macro je strukturální workflow, ne jednorázová preference.

### Příště

- **∑E v STATS panelu** — display KE+PE jako totální energii + drift indikátor (např. delta vůči startu). Jediný integrátor diagnostický signál, který přímo říká „je to OK".
- **Vyhodnotit `GRAVITY_CUTOFF_FACTOR`** — pro fázi 6+ (rozprostřený plyn) zvážit 8-10·ε. Měřit perf vs. kvalita aproximace na P500/P1000.
- **Pages deploy verifikace** — dluh ze sezení 1, **stale Příště 5 sezení v řadě → DO nebo DROP**.
- **Volba (b) FixedJoint API + E3** nebo (c) UI dluhy.
- Vizualizace centroidu křížek (kosmetika).

## Sezení 6 — Pages verifikace, ∑E + Δ∑E indikátor, rotační KE

### Diskuse

- @BEGIN audit: 5 sezení dosud, žádný práh auditu nepřekročen. Stale Příště: **Pages deploy verifikace** 5 sezení po sobě → DO nebo DROP.
- Triáž: nejdřív (A) Pages DO/DROP (vyřešit krátké), pak (B) ∑E indikátor (sezení 5 explicitně označilo jako další krok). (C)/(D)/(E) jako samostatná sezení.
- Q1 (Δ∑E format): jen abs (`E - E₀`), nebo i relativní `|ΔE/E0|`? Volba (a) jen abs — izomorfní s ∑P/∑L pattern, robustní vůči `E₀ ≈ 0` (uvolněný systém).
- Po implementaci a verifikaci E7g: nález **rotační KE chybí v `computeDiagnostics`** — pixely s `rs ≠ 0` (po fázi 3 kontaktech) by ∑E falešně driftovalo. Volba: doplnit teď (čisté), ne čekat na bug.

### Rozhodnutí

- **(A) Pages = DO**: verifikace ukázala, že vše funguje. Ne DROP. Žádné code změny — jen zaprotokolovat a zavřít stale.
- **(B) ∑E indikátor**: dva řádky v STATS pod ∑L. `sumE`, `deltaE` jako `$state`. Capture E₀ při prvním display ticku, kdy `simTime > 0` (PE platná z předchozí `stepGravity`). Reset v `resetScene` → `e0 = null`. Pro rapier mode `lastPE` zůstává 0 (G=0 v E1/E2 → fyzicky správně).
- **Rotační KE**: doplnit v `computeDiagnostics` v prvním pass. Term `½·(m/6)·ω²`. Důvod-doc komentář: I=m/6 pro pixel-čtverec o straně 1 U (∫∫ (x²+y²) dm přes [-0.5,0.5]² = m/6).

### Kód

**Pages verifikace (žádný kód):**
- `gh run list --workflow=deploy.yml`: 5/5 success.
- `Invoke-WebRequest https://mrklas69.github.io/Pixelodynamics/`: 200 OK, valid HTML, base path `/Pixelodynamics/` v `<script>` a `<link>`.
- HEAD na JS+CSS bundle: oba 200.

**∑E + Δ∑E (`src/ui/App.svelte`):**
- Nové `$state` `sumE = 0`, `deltaE = 0`. Interní `let e0: number | null = null` (regular let, ne reactive).
- V `loop` display ticku (`if (displayAccum > 0.5)`):
  ```ts
  const e = d.ke + lastPE;
  if (e0 === null && simTime > 0 && w.pixels.length > 0) {
    e0 = e;
  }
  sumE = e;
  deltaE = e0 === null ? 0 : e - e0;
  ```
- `resetScene` rozšířen o `sumE = 0; deltaE = 0; e0 = null;`.
- Markup: 2 nové řádky `<dt>∑E</dt><dd>{fmtSig4(sumE)}…</dd>` + `<dt>Δ∑E</dt>…` s jednotkou `kg·U²/t²`.

**Rotační KE (`src/sim/diagnostics.ts`):**
- První pass loopu rozšířen o `const w = p.body.angvel();` a druhý term:
  ```ts
  ke += 0.5 * (p.m / 6) * w * w;
  ```
- JSDoc na `ke` upraven: „Kinetická energie (translační + rotační)".

**MODEL.md aktualizace:**
- Diagnostika tabulka rozšířena o PE, ∑E. KE row opraven na translační + rotační.
- Doplněn odstavec o symplektickém bounded driftu (`E` osciluje s amplitudou O(Δt²), neutíká) a logice E₀ capture.

### Test/ověření

- `npm run check`: 0 errors, 0 warnings (po obou změnách).
- **E7g modelshot @ t=60.017s** (s novým `pe` polem v diagnostics):
  - `ke = 3.7339351613`, `pe = -6.4838611208`
  - **E = -2.7499259595** (bit-shoda se sezením 5: -2.7500)
  - `px = 0`, `py = -2.78e-17`, `L = 0`
- **STATS panel @ t=60s** (uživatelem přepsáno z UI):
  - `∑P = 2.776e-17`, `∑L = 0` (D2 symetrie zachovaná do f64 roundoff)
  - **`∑E = -2.75`** (= fmtSig4 z -2.7499)
  - **`Δ∑E = -1.195e-3`** — drift rate ~2e-5/s, **záporný** (symplektický Euler drifuje k vázanějšímu stavu = bounded oscillation, ne divergence)
  - FPS = 60
- Diary sezení 5 (`∑E drift 1e-3 / 60s`) reprodukován v reálném čase, bez nutnosti exportovat modelshot a ručně sčítat.

### Censure! sobě

**1. ∑E indikátor jako „dluh ze sezení 5"** — v sezení 5 jsem přidal `pe` do modelshotu, ale ∑E displej v STATS jsem hodil jako TODO `[~]`. Mohlo se to dotáhnout v jednom kroku — komplexita byla pět minut práce, ne nový sprint. Default to **dotažené featury**, ne polotovary s `[~]` markerem.

**2. Rotační KE nedoplněno preventivně.** Když jsem v sezení 4 přidal `(p.m/6)·w` do `∑L` (a poznámkou v MODEL.md uvedl, že spin je nezbytný kvůli `rs ≠ 0`), nepoložil jsem si symetrickou otázku „a co rotační KE?". Izomorfismus chyběl. Memory `feedback_sanity_check` se vztahuje i na **úplnost diagnostických veličin při rozšíření modelu** — když přidáš spin člen do jedné rovnice, projdi všechny ostatní stejné rodiny.

**3. Diary jako příklad přebujelé prózy** — záznam sezení 5 měl ~120 řádků v hlavní části + ~30 řádků na sub-kapitolu „smoothstep math". Sezení 6 by se mělo vejít do ~50 řádků. Tendence k narativnímu rozpracování auditu/závěrů vede k tokenovému zatížení v každém budoucím @BEGIN.

**Kudos! uživateli (otázka „Co je za veličinu E?")** — indikátor v UI mě posunul k tomu, abych jen napsal čísla; uživatel požadováním definice mě donutil zformulovat KE = translační + rotační pro pixel-čtverec. Tím se odhalilo, že KE rotaci ignoroval. Tohle byl katalyzátor pro fix #2 výše. Bez té otázky by rotační KE chybělo dál.

### Příště

- **Volba mezi:**
  - **(C) Vizualizace centroidu křížek** — kosmetika, krátké, malý ale viditelný UI feature.
  - **(D) Vyhodnotit `GRAVITY_CUTOFF_FACTOR`** — perf benchmark P500/P1000 s factor=5/8/10.
  - **(E) FixedJoint API + E3 + (α/β/γ) test** — otevírá fázi 3, plný experimentální protokol.
- Konzervativní volba: (D) jako numerická příprava před fází 6+ (rozprostřený plyn). (E) je samostatné velké sezení.
- Centroid křížek bude relevantní až s fází 3 (composite objects), do té doby low priority.

## Sezení 7 — Centroid křížek, cursor v HUD, cutoff factor benchmark

### Diskuse

- @BEGIN audit: 6 sezení, žádný práh nepřekročen. **Stale Příště „Vizualizace centroidu křížek" 5 sezení v řadě** → DO nebo DROP.
- Uživatel zvolil DO + sloučil s drobností: cursor world pos v HUD (1. řádek statusbaru).
- Po centroidu `M(PB1000, cutoff=8) = 20 FPS` — uživatel správně poukázal, že měřil bez specifikace času, a v t=2s je systém v jiném stavu než v t=20s. Protokol upraven na `M(preset, cutoff, t) = FPS` s explicit simTime.
- Edukativní moment: dotaz „Čekal bych centroid v L1, ne?" — odpověď: NE, centroid (vážený těžiště poloh) ≠ Lagrange L1 (rovnováha sil). Pro E6 setup centroid ≈ -8.83, L1 ≈ +7.43.

### Rozhodnutí

- **Centroid jako CSS overlay** (ne WebGL draw call). `<div class="centroid">` se ::before/::after pseudoelementy = 16×16 screen-fixed križek modrý #6f8ec1. Pozici z `worldToScreen` (per-frame, O(N) `computeCentroid`).
- **HUD split na 2 řádky** přes `flex-direction: column`. 1. řádek `cur x y` (cursor world), 2. řádek lock/x/y/zoom.
- **Cutoff factor jako runtime parametr** — `GravityParams.cutoffFactor` místo compile-time konstanty z `params.ts`. Slider v SETTINGS (3-12·ε, step 0.5, default 5). Konstanta v `params.ts` zůstává jako zdroj defaultu.
- **Benchmark presety PB500/PB1000** s deterministickým grid spawnem (cols × rows, spacing 3 U), stopAtTime 30 s aby pokryl rovnoměrnou + kolabovanou fázi.

### Kód

**Centroid + cursor (`App.svelte`, `diagnostics.ts`, `camera.ts`):**
- `diagnostics.ts:8` — `computeCentroid(world): {cx, cy} | null` lehká per-frame verze (O(N) jeden pass), vrací null pro prázdný world.
- `camera.ts:34` — `worldToScreen(cam, viewport, wx, wy)` inverze screenToWorld. Ověřeno algebraicky.
- `App.svelte` — `$state cursor` (update v onPointerMove, clear v pointerleave), `$state centroidScreen` (update per frame v render loop). Reset v resetScene.
- CSS: `.hud { flex-direction: column }`, `.centroid { ::before/::after = 16px crosshair }`.

**Cutoff benchmark (`gravity.ts`, `App.svelte`, `presets.ts`):**
- `gravity.ts:24` — `GravityParams.cutoffFactor` přidán; `accumulateForcesGrid` čte z `p.cutoffFactor` místo z importovaného `GRAVITY_CUTOFF_FACTOR`.
- `App.svelte` — `$state cutoffFactor = GRAVITY_CUTOFF_FACTOR`, slider v SETTINGS panelu, propagace do `stepGravity` callu, reset v `applyPreset`.
- `presets.ts` — nové presety **PB500** a **PB1000**, helper `pbSpawn(api, n, spacing)` vyrobí cols × rows grid centred kolem (0,0). Pixely v klidu (vx=vy=0, rs=0, m=1).

### Test/ověření

`npm run check`: 0 errors, 0 warnings. Stejně po přidání cutoff slideru.

**`M(PB1000, cutoff, t) = FPS`:**

| t (s) \\ cutoff | 5 | 8 | 10 |
|---|---|---|---|
| 5  | **60** | **60** | 21 |
| 10 | 58 | 24 | 21 |
| 20 | 20 | 20 | 20 |
| 30 | 20 | 20 | 20 |

PB500 cutoff=5 @ t=5 → 60 FPS (komfort).

**Tři vzorce:**
1. **Density >> cutoff v kolapsované fázi.** Po t≈20 s se shluk stáhl do centra, density v centrálních buňkách překročí limit nezávisle na cutoff hodnotě. Všechny tři běhy konvergují k 20 FPS.
2. **Cutoff má smysl jen v rovnoměrné fázi.** Strop spacing-vs-cutoff ratio je ~3× pro 1000 pixelů (c=10/spacing=3 už 21 FPS od začátku).
3. **Strop 20 FPS = vsync schod** (60/3). requestAnimationFrame zaokrouhlí frame_time 33-50 ms na nejbližší 1/60 dělič.

**Edukativní moment — centroid vs L1:**
- Centroid E6 = (30·(-10) + 1·10 + 1·7.43)/32 ≈ **-8.83** (blízko clusteru).
- L1 ≈ **+7.43** (blízko slabšího singletu, kde se gravitace vyruší).
- Vážený průměr poloh ≠ rovnováha sil. Pro fázi 3+ s composite objects bude centroid per-object relevantní.

### Rozhodnutí (konec sezení)

- **Default cutoff=5 zůstává.** Pro klastrovou orbital dynamiku adekvátní.
- **Strop 1000 pixelů @ 60 FPS je density-bound, ne cutoff-bound.** Cesta dál: Barnes-Hut tree (hierarchický grid) nebo GPU compute (sezení 1 IDEAS). Uniform grid v kolapsované fázi má teoretický limit ~125 pixelů per cell.
- **Pro fázi 6+ rozprostřený plyn** (rovnoměrná density): cutoff lze libovolně zvýšit bez perf hitu. To je explicit: pokud bude scénář držet pixely rovnoměrně rozložené, cutoff=8-10 funguje pohodlně.

### Censure / Kudos

**Censure! sobě:** Při prvním návrhu benchmarku (5 s stopAtTime, vágní „FPS odečíst") chyběl explicit time-axis protokol. Uživatel správně podotkl, že měření v t=2s vs. t=20s = jiný systém. Default to **strukturované zadání s explicit time argumentem** pro každý perf benchmark gravitační simulace, kde se scénář mění v čase.

**Censure! sobě 2:** Rovnice ratio práce `(cutoff/spacing)⁴` predikovala cutoff=8 FPS=10, naměřeno 20 — přehlédl jsem vsync schody. Pro budoucí benchmarking očekávat hodnoty na 60Hz monitoru jen v sadě {60, 30, 20, 15, 12} a interpretovat odpovídajícím způsobem.

**Kudos! uživateli:** Dotaz na centroid v E6 byl nečekaný (centroid je systém-level diagnostika, L1 je per-test particle), ale vedl k vyjasnění důležitého konceptuálního rozdílu, který bude relevantní v fázi 3+ (per-object centroid křížky). Plus návrh formátu `M(PB1000, c=8, 20s)` byl klíčový — bez time-axis by celý benchmark byl nepoužitelný.

### Příště

- **Volba mezi:**
  - **(E) FixedJoint API + E3 + (α/β/γ) test** — otevírá fázi 3, plný experimentální protokol (samostatné velké sezení).
  - **(F) Barnes-Hut tree** — odpověď na dnešní strop 1000 pixelů @ kolaps. Hierarchický grid překoná uniform v nehomogenních distribucích.
  - **(G) Kosmetika:** vizualizace centroidu po composite objektech (per-object kříž), jakmile budou objekty.
- (E) je primární cesta projektu (slepování pixelů byla původní motivace fáze 3). (F) je optimalizační odbočka — má smysl jen pokud benchmark v plánu vyžaduje > 1000 kolabovaných pixelů.
